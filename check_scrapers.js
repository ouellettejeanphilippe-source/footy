import { scrapeMatchFlux } from './js/scrapers.js';
import { S } from './js/state.js';

// we just mock a match and try calling the scrapers?
// actually wait, playright tests already covered verifying the endpoints.
// The user asked to make sure all scrapers are always used and visible in manual fallback.
