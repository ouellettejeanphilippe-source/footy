<<<<<<< SEARCH
/* OVERLAY */
.ov { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 24px; padding: 40px; }
.spinner { width: 56px; height: 56px; border-radius: 50%; border: 4px solid rgba(255,255,255,0.1); border-top-color: var(--accent); animation: spin 1s cubic-bezier(0.5, 0.1, 0.4, 0.9) infinite; }
@keyframes spin { to { transform: rotate(360deg); } }
.ov-msg { font-size: 18px; font-weight: 600; color: var(--text); text-align: center; max-width: 320px; text-shadow: 0 2px 4px rgba(0,0,0,0.5); }
.step { display: flex; align-items: center; gap: 12px; font-size: 14px; font-weight: 600; color: var(--muted); transition: all 0.3s; background: rgba(255,255,255,0.05); padding: 8px 16px; border-radius: 12px; }
.sic { width: 20px; height: 20px; flex-shrink: 0; }
.sic.ok { color: var(--accent-green); }
=======
/* OVERLAY */
.ov { display: flex; flex-direction: column; position: fixed; bottom: 20px; right: 20px; z-index: 9999; background: rgba(10, 10, 12, 0.95); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border: 1px solid rgba(255,255,255,0.1); box-shadow: 0 10px 30px rgba(0,0,0,0.8); border-radius: 16px; padding: 16px 20px; gap: 12px; align-items: flex-start; max-width: 320px; }
.spinner { width: 24px; height: 24px; border-radius: 50%; border: 3px solid rgba(255,255,255,0.1); border-top-color: var(--accent); animation: spin 1s cubic-bezier(0.5, 0.1, 0.4, 0.9) infinite; flex-shrink: 0; }
@keyframes spin { to { transform: rotate(360deg); } }
.ov-msg { font-size: 14px; font-weight: 700; color: var(--text); display: flex; align-items: center; gap: 12px; margin-bottom: 4px; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 12px; width: 100%; }
.step { display: flex; align-items: center; gap: 10px; font-size: 12px; font-weight: 600; color: var(--muted); transition: all 0.3s; padding: 4px 0; width: 100%; }
.sic { width: 16px; height: 16px; flex-shrink: 0; }
.sic.ok { color: var(--accent-green); }

@media (max-width: 768px) {
    .ov { bottom: 80px; right: 50%; transform: translateX(50%); width: calc(100% - 32px); max-width: 400px; padding: 12px 16px; }
}
>>>>>>> REPLACE
