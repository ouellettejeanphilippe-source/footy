
## En cours

## Fait
- 2026-09-09 - **« Des améliorations à suggérer ? » puis « Go ».** Audit du dépôt en trois relevés (structure du code ; CI, PWA, performance, sécurité ; tests et pipeline de données), puis correction de ce qui était AVÉRÉ, en laissant les chantiers de structure (cycle d'imports par `js/config.js`, fonctions géantes de `js/ui.js`, listes ESPN dupliquées avec `scripts/scrape_schedule.mjs`, harnais de tests commun, adaptateurs `js/sources/` sans test, interface classique) pour des sessions dédiées.
  - **La promotion des miroirs n'a jamais fonctionné.** `scripts/scrape_streams.mjs` ne posait le gagnant que s'il était absent, or `fetchWithMirrors` le pose toujours après un succès : le cas sain tombait dans le `else` et remettait le gagnant à null, `shouldPromoteSource` refusait tout, et le bot n'a jamais réécrit `domains.json` (dernier commit dessus : un humain, le 7 septembre). `gagnantApresLecture` (`js/config.js`, fonction pure) fixe la règle : l'adresse qui a répondu, à condition d'avoir livré des matchs. `unit_mirrors` verrouille le cas sain jusqu'à la promotion.
  - **Aucun garde-fou avant l'écriture de `data/streams.json`.** Le calendrier refusait déjà d'écrire une passe vide et fusionnait une passe partielle ; les liens, non : onze sources en échec (trois l'étaient ce jour-là) auraient publié un fichier vide pour tous les appareils. Désormais, aucune source vivante → le fichier n'est pas touché ; passe partielle → les matchs des sources muettes sont repris du fichier publié, sans doublon, à partir de la veille (`conserverSourcesMuettes`, `js/config.js`, test `unit_passepartielleliens`). Une date antérieure à la veille est écartée (un « 2016-12-01 » traînait dans le fichier).
  - **Cadence réelle du workflow des liens.** Planifié à :17 et :47, il n'est exécuté par GitHub que cinq à six fois par jour (00:15, 04:48, 09:46, 13:57, 17:50, 20:56 le 9 septembre), toutes les exécutions réussies : les crons fréquents sont retardés ou sautés. Ce n'est pas corrigeable sans déclencheur externe (jeton) ; le workflow, le README, `AGENTS.md` et l'architecture disent maintenant la cadence réelle au lieu de la cadence promise.
  - **Workflow calendrier relancé à chaque push sur `main`**, sans `concurrency` ni délai maximal : chaque fusion relançait un scrape ESPN complet, d'où la course décrite dans ses propres commentaires. Déclencheur `push` restreint aux fichiers qui produisent le calendrier, `concurrency` sans annulation, 20 min max.
  - **Gains rapides.** `npm test` passe par `node --test tests/*.test.js` (`test:unit`) : 50 fichiers en 17 s au lieu de 38 en série, découverte par motif (plus de chaîne de 49 `&&` à tenir à jour), un échec n'arrête pas les autres ; les tests qui ouvrent jsdom sur le noyau doivent finir par `process.exit(0)`, sinon les minuteries du noyau gardent le processus en vie. `.nvmrc` et `engines.node >= 22` ; les quatre workflows lisent `.nvmrc`. Chromium mis en cache d'une exécution à l'autre (`~/.cache/ms-playwright`, clé sur le lockfile). `tests.yml` et `domains-watch.yml` en lecture seule (`permissions: contents: read`) avec un délai maximal. `sw.js` ne précache plus `data/*.json` (1,2 Mo par version, périmés en trente minutes ; le gestionnaire `fetch` les range à la première lecture) → `sports-guide-v17`, `VERSION_APP` en phase. `manifest.json` : `theme_color` aligné sur la balise d'`index.html` (`#0a0a0c`), `id` et `scope` ajoutés. `index.html` et `legacy.html` annoncent les huit modules les plus lourds en `modulepreload`.
  - **Révélé par la suite Playwright du jour : un favori était rendu deux fois avec le même `id`.** « Un match terminé selon ESPN quitte le Live » tombait déjà sur `main` (reproduit sur un arbre sans mes changements, 2 échecs sur 2) : deux cartes disparaissaient au lieu d'une. Le match choisi, CF Montréal (favori par défaut) en cours, figure dans la section Favoris ET dans la section Live, les deux cartes avec `id="mb-<id>"`. Or `getElementById` ne voit que la première : la mise à jour des scores en place (`processChunk`, `js/main.js`) cherchait déjà `mb-<id>_fav_copy` pour la copie, et `getOriginalMatchId` (`js/ui.js`) savait déjà retirer ce suffixe, mais plus personne ne le posait depuis la refonte des cartes — la carte de la section Live d'un favori ne suivait donc plus les scores. `buildEPG` (`js/ui.js`) pose de nouveau `_fav_copy` sur la section Favoris ; `liveViewIsStale` (`js/main.js`) et le repli d'une ligue (`js/utils.js`) retirent le suffixe avant de chercher le match. Le test exige désormais des identifiants tous distincts et compte les cartes du match choisi.
  - Fichiers : `js/config.js`, `scripts/scrape_streams.mjs`, `js/ui.js`, `js/main.js`, `js/utils.js`, `sw.js`, `js/multiview.js`, `manifest.json`, `index.html`, `legacy.html`, `package.json`, `.nvmrc`, les quatre workflows, `tests/unit_mirrors.test.js`, `tests/unit_passepartielleliens.test.js` (nouveau), `tests/test_app_boot.spec.js`, `README.md`, `AGENTS.md`, `docs/ARCHITECTURE.md`.
  - Reste (relevé par l'audit, non fait ici) : sortir les 630 lignes d'interface Stats de `js/config.js` pour casser le cycle d'imports ; partager `ESPN_LEAGUES` et les aides de date entre `js/api.js` et `scripts/scrape_schedule.mjs` (la table d'équipes du script n'a que six entrées) ; un harnais de tests commun (le montage jsdom est recopié dans 38 fichiers) ; un test par adaptateur `js/sources/` ; suspendre les minuteries sur `visibilitychange` ; onglets sans `role="tab"` et fiche sans capture du focus ; aucune CSP ; la recherche globale sans champ et la case sans « hier ».
- 2026-09-08 - **« Est-ce que les scores peuvent se rafraîchir au rafraîchissement ? Ça devrait être pas mal en direct, les scores. »** Le bouton « ↻ Actualiser » les redemandait déjà ; ce qui manquait, c'était l'ouverture et la cadence. La passe qui porte les scores reconstruit TOUTE la journée (47 chemins ESPN plus les calendriers annexes), d'où ses cinq minutes ; et à l'ouverture, la grille vient souvent du calendrier rangé en local, qui peut avoir dix minutes. Un score pouvait donc traîner un quart d'heure.
  - **Une passe ciblée** (`rafraichirScoresEnDirect`, js/api.js) : un score ne demande que les ligues qui ont un match EN COURS — une à cinq adresses. Chaque minute, et 1,5 s après le démarrage, le temps que `S.matches` soit posé. `tachesEnDirect` rend les couples (chemin, jour) distincts : le chemin est désormais mémorisé sur le match (`espnPath`, posé par `processEspnData`), avec le nom de la ligue en repli pour les calendriers rangés avant ce changement ; le jour est celui DU MATCH, pour qu'un match d'hier soir encore en cours soit demandé sous la date d'hier. `etatsDepuisEspn` réduit la réponse à ce que les cartes affichent, sans reconstruire équipes ni logos ; pour une épreuve, c'est l'état de la séance qui compte.
  - **`majScoresDansCache`** : les scores frais sont aussi rangés dans `api_calendar_cache_<jour>`. Sans cela, la passe complète suivante relit ce calendrier et ramène les scores d'avant — `mergeFluxToApi` reporte `streamsLoaded` d'un état à l'autre, mais pas les scores.
  - Tests : `tests/unit_scoresdirects.test.js` (8 groupes) et « les scores des matchs en cours se rafraîchissent chaque minute, sans redemander toute la journée » (Playwright, horloge avancée d'une minute). **Vérifié par sabotage** : la passe rapide désarmée, le score reste à 3-1 après une minute ; armée, il passe à 4-1, la carte suit et le calendrier local aussi — et la ligue sans match en cours n'est pas redemandée.
- 2026-09-08 - **« Les boutons se cachent pas en fenêtre externe ? »** Non, et jamais. Au bout de trois secondes sans geste, la barre et les en-têtes de tuiles (↗ Site, ⋮, ✕, ⤢, numéro, source) s'effacent pour ne pas rester posés sur la vidéo. Ce mécanisme cherchait ses éléments sous `#mv-container` et n'écoutait que lui. Or « Fenêtre détachée » (`toggleDocumentPiP`) DÉPLACE `#mv-grid-wrapper` — donc toutes les tuiles — dans le document d'une autre fenêtre : plus aucun geste fait là-bas n'atteignait `#mv-container` pour réarmer le compte à rebours, et `mvContainer.querySelectorAll('.mv-hdr')` n'y trouvait plus rien à cacher.
  - `js/multiview.js` : `grilleDuLecteur()` (la grille, dans la page ou dans la fenêtre détachée), `grilleDetachee()` et `appliquerRepos(grille, barre, cacher)` — on agit sur la GRILLE, pas sur le conteneur. `toggleDocumentPiP` fait écouter le document de la fenêtre détachée et réarme au retour. Détachée, la grille remplit sa fenêtre : on efface comme en plein écran ; réduite dans la page, les commandes restent, c'est voulu.
  - L'API Document Picture-in-Picture n'existe pas dans le navigateur des tests (vérifié : `documentPictureInPicture` absent). `tests/unit_reposlecteur.test.js` (3 groupes) reproduit donc ce qu'elle fait — déplacer la grille dans un autre document — et vérifie que les en-têtes s'effacent quand même, en constatant au passage que le conteneur n'en contient plus aucun : c'était exactement la panne.
  - `sw.js` → `sports-guide-v16`, `VERSION_APP` en phase.
- 2026-09-08 - **« ESPN, les scores sont parfois là, parfois pas là. »** Ce n'était pas les scores : c'étaient des **ligues entières** qui disparaissaient puis revenaient, leurs scores avec.
  - **La cause.** Le calendrier du jour vient de 47 chemins ESPN interrogés en parallèle. Le garde-fou posé le 7 septembre ne couvrait que le cas où AUCUN n'avait répondu (`reponses === 0`). Quand 46 répondaient et un échouait, le résultat — amputé de la ligue muette — était écrit tel quel comme LE calendrier du jour (`api_calendar_cache_<jour>`) et renvoyé à l'appelant. Le rafraîchissement des scores passe précisément par là, toutes les cinq minutes et à chaque retour au premier plan. La passe suivante lisait ce calendrier amputé, `setMatches` remplaçait la grille, et les matchs de cette ligue disparaissaient ; au tour d'après leur chemin répondait et ils revenaient.
  - **Pourquoi les échecs partiels sont la règle, pas l'exception.** Les 47 requêtes partaient d'un coup vers le même hôte. Un navigateur n'ouvre que six connexions par hôte : les autres attendent. Or `AbortSignal.timeout(8000)` compte à partir de la **création** du signal, pas de l'envoi — sur un réseau lent, la file mange le délai et les dernières requêtes expirent sans avoir jamais été envoyées. D'où le caractère intermittent et dépendant de l'appareil. Et depuis la relecture de la veille (ce matin), c'était **94** requêtes simultanées entre minuit et 06:00 : ma propre régression, la nuit était la pire fenêtre.
  - `js/api.js` : `ESPN_CONCURRENCE` (6) et `enPiscine(items, taille, faire)` — une seule file pour le jour et la veille, une tâche qui échoue n'arrête pas sa voie (sans ce filet, une réponse illisible privait la passe de tous les chemins restants de sa voie). `fusionnerPassePartielle(frais, cache, jour)` : ESPN fait foi sur ce qu'il a renvoyé, le calendrier connu est conservé pour le reste, seule une passe **complète** élague. Clé d'appariement : l'identifiant, ou équipes + date + heure à défaut. `window.calendrierInfo.source` vaut `espn (partiel)` dans ce cas.
  - `scripts/scrape_schedule.mjs` : mêmes deux règles. Un passage partiel publiait sinon un `data/schedule.json` amputé pour TOUS les appareils jusqu'au suivant — la moitié serveur de la même panne.
  - Tests : `tests/unit_espnpartiel.test.js` (8 groupes, ajouté à `npm test`) et « une passe ESPN partielle ne fait pas disparaître les ligues muettes ni leurs scores » (Playwright, deux ligues, un chemin qu'on fait taire). **Vérifié par sabotage** : sans le correctif, le calendrier du jour tombe à `["espn_mlb1", "wwe_nxt_…"]` — le match NBA a disparu après un seul chemin muet.
  - `sw.js` → `sports-guide-v15`, `VERSION_APP` en phase.
  - Reste : un match réellement retiré par ESPN survit jusqu'à la prochaine passe complète. C'est le prix, très inférieur à la perte de ligues entières.
- 2026-09-08 - **« Mettre à jour toute la documentation, claire, fonctionnable, complète. »** Les quatre documents sont réécrits d'après le code, pas d'après leur version précédente (deux relevés exhaustifs, modules et interface, chaque fait vérifié dans le source) :
  - `README.md` : ce que fait l'application, comment l'utiliser (installation PWA, script utilisateur en trois étapes, d'où viennent les données), comment développer (commandes, arborescence, workflows, règles à retenir). Plus de section APK, plus de lien vers un dictionnaire de fonctions périmé.
  - `docs/ARCHITECTURE.md` : référence technique **par sujet** (vue d'ensemble, arborescence, modules et cycle d'imports, démarrage et minuteries, calendrier, liens, lecteur, appariement, interface, stockage, service worker et version, scripts et workflows, tests, conventions). Les sections datées qui s'y étaient empilées depuis avril 2024 sont retirées : leur contenu vit dans ce journal, et ce qui en reste vrai est fondu dans les sujets.
  - `FEATURES.md` : l'interface onglet par onglet, avec les libellés exacts, ce que chaque commande fait, et ce qui n'existe **que** dans l'interface classique (date, recherche, pastilles de ligues, « Liens manquants »).
  - `AGENTS.md` : règles remises d'aplomb (plus de Python, de `check_*.py`, d'`express`, ni de « 9000 lignes ») ; interdits complétés (pas de `sandbox`, pas de test dépendant du réseau ou de l'heure, une instabilité n'est pas un flocon).
  - Au passage, quatre écarts révélés par les relevés : la case « 🎩 Reconstruire les pages non intégrables » (`index.html`, `legacy.html`) n'était lue par aucun code, retirée ; le corps inline de la balise `<script src>` de `legacy.html` (jamais exécuté) retiré ; le commentaire « every 60 seconds » de `js/main.js` disait le contraire des 5 minutes réelles ; l'état vide du Live promettait des « flèches de date » que la coquille actuelle n'a pas.
  - Reste : la recherche globale (`setSearchQuery`, `S.searchQuery`) n'a plus de champ dans aucune des deux interfaces ; son code reste en place, inaccessible.
- 2026-09-08 - **« Go, clean tout. »** Inventaire du code mort, chaque candidat vérifié par grep (aucune référence hors de lui-même), puis retrait :
  - Racine : `t.mjs`, `run_tests.js`, `run_url_tests.js`, `start_app.sh`, `screenshot.py`, `run_checks.py` (aucun workflow n'installe Python ; il ne vérifiait que lui-même), `pnpm-lock.yaml` (tous les workflows font `npm ci` ; le fichier était désynchronisé), `videos/` (deux webm jamais référencés), `.jules/` (notes d'un agent de 2024), `ROADMAP.md` (demandait de scinder un `index.html` de 9000 lignes, fait depuis longtemps), `SUIVI_FONCTIONS.md` (déclaré non fiable ici même le 3 septembre ; lien retiré du README).
  - Chantier abandonné du « repli JSON » : `tests/verify_fallback.mjs` et `docs/PROMPT_FOR_FALLBACK_ACTION.md`, remplacés depuis par `scripts/scrape_streams.mjs` et `data/streams.json`.
  - Dépendances jamais importées : `acorn`, `acorn-walk`, `dotenv`, `node-fetch` (les scripts serveur utilisent le `fetch` natif de Node 22). `package.json` n'a plus que des dépendances de développement ; champ `main` (qui pointait sur le script Tampermonkey) retiré, description remplacée, et `npm run scrape:streams` reçoit la même limite mémoire que le workflow (`--max-old-space-size=8192`, sans quoi la passe meurt à 4 Go).
  - Symboles JS morts : `parseWWEIcs` (la WWE vient du JSON de wwe.com), `recordEmbedResult` (la doc affirmait que `fallbackToIframe` l'appelait : faux), `appTheaterTimer`, `currentGlobalStatsMatchId`, `menuEstOuvert`.
  - Docs remises d'aplomb : `AGENTS.md` (plus de `check_db.py`/`check_js.py`/`express` ni de « 9000 lignes »), `docs/ARCHITECTURE.md` (stack, arborescence, section `run_checks.py`, registre d'intégrabilité).
  - Gardé après vérification : mode TV (`tv.css`, `js/tv-navigation.js`), interface classique, `domains.json`, icônes, `FEATURES.md`, `jsdom`, `playwright` (pilote Chromium dans `scripts/verify_players.mjs`) et `@playwright/test`, les quatre workflows, tous les modules `js/`. La recherche « Liens manquants » (`js/links.js`) n'a de bouton que dans l'interface classique : elle part avec elle, pas avant.
- 2026-09-08 - **« Retirer complètement » l'APK Android.** « Est-ce que l'apk bloque pubs, pop-up et inclut les fonctions du script Tampermonkey ? » Non, rien des trois : `MainActivity.java` héritait de `BridgeActivity` sans une ligne de plus, `capacitor.config.json` ne portait que le nom, le workflow ne copiait même pas le script dans `www/`, et une WebView ne sait pas exécuter un userscript. L'APK était le site ouvert dans une WebView sans extension, avec une icône en plus, et sans mise à jour automatique — moins que la PWA. Son workflow (JDK, Gradle) tournait à chaque PR pour un artefact que personne n'installait.
  - Retirés : `android/` (53 fichiers), `capacitor.config.json`, `.github/workflows/android-build.yml`, `@capacitor/android`, `@capacitor/cli`, `@capacitor/core` (`package-lock.json` régénéré par `npm install --package-lock-only`), l'entrée `www/` du `.gitignore` et la section « Création de l'application Android (APK) » du README.
  - Si l'idée revient : la seule raison d'être d'un APK serait d'injecter le script de nettoyage dans la WebView côté Java (il faut intercepter le HTML de chaque iframe de lecteur), ce que le navigateur mobile ne permet pas. Rien de cela n'existait.
- 2026-09-08 - **« Ya des matchs qui finissent dans la nuit. »** Relevé sur `data/schedule.json` du 7 septembre : Athletics – Blue Jays à 22:05 (3 h de base-ball, fin vers 01:05). Un match porte la date de son coup d'envoi, et l'application ne montrait que `m.matchDate === aujourd'hui` — à cinq endroits de js/main.js. À 00:00, « aujourd'hui » change : le match disparaissait de la grille et du Live, ESPN n'était plus relu que pour la date du jour (score figé, puis « Fin ? » par présomption alors que ça joue encore), et le cache serveur des liens était filtré sur la même date. Seul l'onglet ouvert depuis le soir tenait, tant qu'on ne le rechargeait pas.
  - **`js/nuit.js`** (sans import) : la règle, « un match d'hier qui déborde sur cette nuit fait partie de la journée d'aujourd'hui » — `appartientAuJour`, `debordeSurLaNuit`, `nuitEnCours` (avant 06:00), `veille`/`lendemain`, `minutesDansLaJournee`/`comparerHeures` (hier soir passe avant 00:00), `memeMatchATraversLaNuit` (un flux relu après minuit par le serveur porte la date du jour : même heure de coup d'envoi, jours consécutifs → même match).
  - `js/api.js` : pendant la nuit, ESPN est relu pour la veille et seuls les matchs qui débordent sont gardés ; le rafraîchissement des scores en profite. `scripts/scrape_schedule.mjs` : même chose pour le cache serveur produit avant 06:00 (`traiterEspn`). `js/main.js` : les cinq filtres et celui des liens passent par `appartientAuJour`. `js/ui.js` : case dessinée à 00:00 sur ce qui reste, tris par rapport au jour affiché. `js/match.js` : dates différentes tolérées par `memeMatchATraversLaNuit`.
  - `sw.js` → `sports-guide-v14`, `VERSION_APP` en phase. Tests : `tests/unit_nuit.test.js` (7 groupes, ajouté à `npm test`) et « la nuit appartient à la veille : le match de 22:05 est encore là à 00:30, dans le Live, la grille et les scores » (Playwright, ESPN simulé pour le jour ET la veille ; vérifié par sabotage : sans les changements, la veille n'est jamais demandée et le test tombe).
  - **Les 23 tests de démarrage tombaient déjà avant ce changement** (vérifié par remise à zéro des fichiers JS) : `data/streams.json` était du 8 et `data/schedule.json` du 7, l'instant figé par `instantDesDonnees` tombait le 8, le calendrier du 7 était « périmé », ESPN refusé — grille vide. C'est le même sujet, vu du dépôt : entre minuit et la passe quotidienne de 05:00, les deux caches ne sont jamais datés du même jour. `instantDesDonnees` (`tests/test_app_boot.spec.js`) part désormais du calendrier (`fetchDate` de `data/schedule.json`) et balaie CE jour-là ; `data/schedule.json` a été régénéré pour le 8 (`node scripts/scrape_schedule.mjs`).
  - Au passage, révélé par le calendrier du jour : `updateMatchUiAfterScrape` (js/scrapers.js) ne retrouvait plus la barre d'actions de la fiche (`.flux-head` depuis la refonte ; l'ancien sélecteur sur le style en ligne ne trouvait rien) et la faisait disparaître à la première relecture des flux — le test « la fiche de match a une seule croix… » tombait. Corrigé : `.flux-head` est conservée, et le bouton 🔄 est réactivé une fois la relecture finie.
  - En intégration continue, « chaque section repliable de l'onglet Live répond au clic » tombait une fois sur deux (reproduit en local : 4 échecs sur 6, et 2 sur 6 sans mes changements). Cause : le premier chargement dessine la grille avec les liens déjà connus puis la REDESSINE quand la lecture des sources finit, quelques centaines de millisecondes plus tard ; le test tenait des titres détachés (`nextElementSibling` nul). `buildEPG` (js/ui.js) compte ses rendus dans `window.rendusGrille`, et les tests attendent que ce compteur soit immobile (`attendreGrilleStable`) au lieu d'un délai fixe de 500 ms. Huit répétitions vertes.
  - Reste : la case d'un match d'hier soir affiche « 22:05 » sans dire « hier » ; et un match d'hier terminé dans la nuit reste dans la journée jusqu'à 06:00 (c'est voulu : il en a fait partie), plus après.
- 2026-09-08 - **« Un seul background pour page Live, pas une répétition du même. »** Relevé en listant les éléments de la page qui portent un fond : **cinq `.section-title`, cinq fois le même `linear-gradient(var(--bg) 55%, transparent)`**. Les titres de section sont collants — quand les cartes défilent dessous, il faut qu'ils restent lisibles — et c'était obtenu en REPEIGNANT la couleur de fond. Sur le fond unique de la page (`#app-bg-container`, un dégradé ou un motif selon les Options), cela posait une bande de couleur PLATE, répétée à chaque section : le fond ne se voyait plus, il se répétait.
  - `styles.css` et `styles-legacy.css` : plus aucune copie. Un flou d'arrière-plan (`backdrop-filter: blur(14px)`) détache le titre de ce qui passe dessous **en laissant voir le fond de la page tel qu'il est**, et une ombre portée sur le texte prend le relais quand une affiche claire passe juste dessous. Repli sous `@supports not (backdrop-filter…)` : la bande revient, mais seulement là où le flou n'existe pas.
  - Test : « la page Live n'a qu'un fond : aucune section n'en repeint une copie » (`tests/test_app_boot.spec.js`) — plusieurs sections rendues (sinon le test ne prouve rien), aucun titre avec un `background-image`, tous collants, tous flous, et **un seul** `#app-bg-container`. Vérifié par sabotage du CSS seul : le dégradé remis, le test tombe sur « aucun titre de section ne repeint le fond de la page ». **Suite complète verte, 33 tests Playwright.**
- 2026-09-08 - **« Ajouter logos WWE, F1, etc. »** `getLogo` connaissait déjà ces deux-là, mais sur le nom EXACT (`lowerName === 'raw'`) : « RAW #1737 », le libellé qu'ESPN emploie réellement, n'y répondait pas — et depuis que les doublons du même spectacle sont réunis sur la première entrée, c'est justement celui-là qui restait, avec son carré gris « R# ».
  - `js/db.js` : lecture par MOTS ENTIERS. `\braw\b` reconnaît « raw #1737 » et « wwe monday night raw », et ne reconnaît jamais « crawley town » — un club anglais bien réel qui contient les mêmes lettres (le nom garde ici ses espaces, contrairement à `normName`). Ajoutés : AEW (Dynamite, Rampage, Collision), TNA/Impact, les séances de F1 (« F1 Main Race » et pas seulement les Grands Prix), UFC et PFL.
  - **Les cinq adresses ont été vérifiées une par une** chez ESPN (`leagues/500/…`) : wwe, aew, f1, ufc, pfl répondent 200. NASCAR, MotoGP, IndyCar, boxe, MMA générique répondent 404 — elles ne sont donc PAS inventées, le blason générique vaut mieux qu'un lien mort.
  - `js/api.js` : la réunion des doublons fait suivre `homeLogo`/`awayLogo` de l'entrée écartée, pour que garder la première ne coûte jamais son blason à la carte.
  - Tests : 8 cas ajoutés à `tests/getLogo.test.js`, dont « Crawley Town garde le sien ». Vérifié au rendu : la carte WWE affiche `500/wwe.png`.
- 2026-09-08 - **« Créer favicon. »** Il n'y en avait **aucune** : ni `<link rel="icon">` dans les pages, ni fichier à la racine. Le navigateur demandait `/favicon.ico`, recevait un 404, et l'onglet comme l'écran d'accueil restaient vides. Le manifeste ne portait que deux SVG en data-URI, qu'iOS ignore pour l'écran d'accueil.
  - `icons/favicon.svg` : dessinée dans le langage des cartes — fond bleu nuit en dégradé, liseré indigo, triangle de lecture, et la pastille du direct en dégradé rouge → rose. Une forme pleine, un seul symbole, un accent : elle reste lisible à 16 px (vérifié au rendu).
  - PNG produits depuis ce SVG par Chromium (32, 180, 192, 512) : `favicon-32` pour les navigateurs sans SVG, `apple-touch-icon` (180) pour l'écran d'accueil iOS, 192 et 512 pour le manifeste, la 512 servant aussi d'icône « maskable » pour qu'Android ne la rogne pas de travers.
  - Déclarée dans `index.html` **et** `legacy.html`, pointée par `manifest.json` (plus aucun data-URI), pré-cachée par `sw.js`. `CACHE_NAME` et `VERSION_APP` → `sports-guide-v13`.
  - Tests : `tests/unit_favicon.test.js` (4 groupes, ajouté à `npm test`) — les fichiers existent et **les PNG font bien la taille annoncée** (signature et en-tête IHDR lus, un fichier au bon nom mais au mauvais format ne se verrait pas autrement) ; les deux pages les déclarent ; le manifeste pointe vers des fichiers présents ; les icônes sont pré-cachées et `VERSION_APP` suit `CACHE_NAME`. Suite complète verte.
- 2026-09-08 - **« Raw, c'est WWE, ça se peut que ça soit pas identifié Raw mais WWE, comme F1. »** Exact, et mesuré le jour même : **une seule soirée portait trois noms**, dont deux venaient d'ESPN lui-même.

    | nom | provenance | liens |
    |---|---|---|
    | `RAW #1737` | ESPN et footybite | 0 |
    | `WWE` / `Raw` | ESPN | 0 |
    | `WWE Monday Night RAW` | sportsurge | **9** |

  Aucun ne s'appariait : **deux cartes pour une seule émission**, et les neuf liens n'atteignaient ni l'une ni l'autre.
  - `spectacleDeCatch(nom)` (js/match.js) dégage le spectacle de la fédération (WWE, AEW, TNA…), du jour de diffusion (« Monday Night ») et du numéro d'épisode — exactement comme un Grand Prix est dégagé de « F1 » et de « Race » plus bas dans la même fonction. Utilisé en tête de la branche « épreuve » de `debugMatchPair` : le spectacle prime sur la comparaison de lettres, car « RAW #1737 » et « WWE Monday Night RAW » n'ont presque rien en commun à lire et sont pourtant la même soirée, tandis que « Raw » et « NXT » se ressemblent davantage et n'en sont pas.
  - **Le piège évité** : `normName` retire les espaces, donc « raw » se retrouve DANS « crawleytown » — Crawley Town est un club anglais bien réel, qui aurait reçu les flux de la WWE. On n'accepte donc un nom de spectacle que dans deux cas nets : le libellé EST ce spectacle une fois le numéro retiré, ou il est précédé du nom de sa fédération.
  - **Les doublons d'ESPN sont réunis** (fin de `mergeFluxToApi`, js/api.js) : deux entrées du même spectacle le même jour n'en font plus qu'une, qui reçoit les liens de l'autre. Vérifié sur les données réelles : une seule carte WWE, avec ses 9 liens (avant : deux cartes, 0 lien).
  - Tests : `tests/unit_spectaclecatch.test.js` (4 groupes, ajouté à `npm test`) — les trois noms du jour, le club « Crawley Town » qui n'est pas un spectacle, les trois entrées qui s'apparient enfin, et NXT / SmackDown / un match de football qui ne s'y joignent jamais.
- 2026-09-08 - **« Background toujours blanc » dans le lecteur.** Vérifié d'abord d'où vient le blanc : la tuile de l'application est déjà noire (`background:#000` sur son conteneur), le blanc vient de la page du site de diffusion **à l'intérieur du cadre**, sous la vidéo. Un document d'une autre origine : aucune feuille de style d'ici ne l'atteint. Ce qui EST à notre portée a été fait — `background:#000` et `color-scheme: dark` sur le cadre lui-même (js/multiview.js) : la zone que la page n'a pas encore peinte (chargement, page vide, lecteur qui n'occupe pas toute la hauteur) est noire au lieu d'être blanche. Une page qui déclare son propre fond blanc reste blanche : c'est le script utilisateur qui la nettoie, ou le bouton ⤢ de la tuile qui recadre sur la vidéo.
- 2026-09-08 - **« Fusion : pas encore faite » — la capture a tranché.** Page Logs d'un téléphone, une minute après le chargement : « Calendrier ✅ 30 matchs · local », « Liens ✅ 241 matchs · généré il y a 45 min », « Stockage local ✅ 19 Ko », « Version ✅ sports-guide-v12 » — et **« Fusion (liens ↔ matchs) : pas encore faite »**, donc toutes les cartes avec la loupe. Ni un fichier manquant, ni un stockage plein, ni une copie ancienne : les liens étaient là, en mémoire, et le rattachement n'avait simplement pas eu lieu.
  - **Ce n'est pas de la lenteur** : la fusion mesurée sur les données réelles coûte **25 ms** (`fusionMs`, js/api.js, désormais affiché dans la page Logs). L'étape n'était pas lente, elle n'était pas ATTEINTE.
  - **La cause.** Quand l'application décide de relire les sources elle-même — script utilisateur présent et dernière passe de plus de cinq minutes, ou cache serveur trop vieux — elle enchaîne une dizaine de pages de liste par proxys CORS **avant** de fusionner quoi que ce soit. Des dizaines de secondes, parfois des minutes sur un téléphone, pendant lesquelles la grille reste celle du cache local, sans liens. Et c'est **aussi l'explication de « je reload les streams sont là, je reload c'est vide »** : selon qu'une passe précédente avait eu le temps d'écrire ses liens dans le cache local, on voyait les liens ou la loupe.
  - **Le correctif** (js/main.js, branche de lecture des sources) : on fusionne les liens DÉJÀ chargés et on dessine tout de suite — 25 ms —, puis la lecture des sources continue derrière et redessine quand elle apporte quelque chose de plus. Rien ne justifiait d'attendre le réseau pour montrer des liens qu'on avait en main.
  - Test : « les liens déjà connus s'affichent avant la lecture des sources, qui peut ne jamais finir » (`tests/test_app_boot.spec.js`). Le script utilisateur est déclaré présent par le vrai échange de messages (`mv_bridge_hello` / `mv_bridge_ready`), ce qui force ce chemin ; les requêtes sortantes sont laissées **en suspens** et non refusées — un refus immédiat ferait finir la lecture en une seconde et le test ne distinguerait rien. Vérifié par sabotage : sans le correctif, « la grille a été dessinée AVANT la lecture des sources » tombe. **Suite complète verte, 32 tests Playwright.**
- 2026-09-08 - **Deux boutons demandés : « Mettre à jour l'app » et « Actualiser ».**
  - **« Ajouter bouton actualiser pour version pwa ? »** Installée sur l'écran d'accueil, l'application est servie par son service worker : une version publiée peut mettre du temps à la remplacer, et recharger la page n'y change rien puisque c'est le service worker qui répond. C'est précisément le cas qu'on soupçonnait quand un appareil ne montrait pas la même chose qu'un autre, et rien ne permettait de forcer. `mettreAJourApplication()` / `viderCachesApplication()` (js/multiview.js) retirent le service worker, vident **tous** ses caches, puis rechargent sur une adresse neuve (`?maj=…`, `location.replace`) pour que le cache HTTP ne réponde pas non plus ; il se réinstalle seul au chargement suivant. **Le stockage local n'est pas touché** : préférences, favoris, calendrier du jour. Offert dans le menu « Plus » (`#btn-maj-app`, index.html) et dans la page Logs, à côté de la ligne « Version » — de quoi comparer avant et après. `VERSION_APP` et `CACHE_NAME` → `sports-guide-v12`.
  - **« Un sur le guide pour avoir scores et streams à jour. »** Depuis que le haut de page a été vidé (6 septembre), il n'existait plus aucun geste pour redemander les données : il fallait attendre la minuterie de cinq minutes, ou recharger. `actualiserMaintenant()` (js/main.js) redemande les deux, dans l'ordre où ils servent — le fichier des liens (`loadPrefetchedStreams(true)`), les scores auprès d'ESPN (`backgroundUpdateGuide`), puis une passe complète qui refait la fusion et redessine. `loadAll(true, true)` plutôt qu'une passe de premier plan, qui viderait `S.matches` et ferait clignoter la grille. Le bouton se désactive et tourne pendant la passe, et le toast final dit le résultat (« À jour : 19 matchs sur 30 ont des liens »).
  - Bouton flottant `#btn-actualiser` (index.html, `.fab-actualiser` dans `styles.css` et `styles-legacy.css`) : en bas à droite, **au-dessus** de la barre d'onglets sur téléphone (réduit à son icône sous 900 px), masqué sur les pages Logs / Options / Script — `body.view-page`, posée par `showPage` (js/utils.js), car il n'y a rien à y rafraîchir.
  - Tests : `tests/unit_majapp.test.js` (4 groupes, ajouté à `npm test`) — service worker retiré et tous les caches vidés, **stockage local intact**, navigateur sans service worker ni caches toléré, bouton présent dans la page Logs et dans le menu ; et un test Playwright « le bouton Actualiser est devant la grille, pas sur les pages, et dit qu'il travaille » (position au-dessus de la barre d'onglets, `display: none` sur Logs, désactivé + animé pendant la passe, grille toujours là après). **Suite complète verte, 31 tests Playwright.**
- 2026-09-08 - **Capture du téléphone : les trois sources vertes, et TOUTES les cartes avec la loupe.** La page Logs disait « Calendrier ✅ 30 matchs · schedule.json » et « Liens ✅ 241 matchs · généré il y a 22 min » — les deux fichiers étaient donc bien arrivés — et pourtant aucune carte n'avait de lien, y compris « San Diego Padres vs Washington Nationals », en direct, que `data/streams.json` sert avec 46 liens. Les scores, eux, étaient justes (3-2).
  - **Non reproductible ici.** Mêmes fichiers, même heure, huit rechargements successifs, en contexte bureau PUIS en contexte téléphone (390×844, tactile, agent iOS) : 241 matchs pré-calculés, 30 dans la grille, **19 avec liens**, et les 46 liens du match en direct bien attachés. Les quatre cartes à la loupe sont les entrées WWE en double, qui n'ont réellement aucun lien. Le défaut est donc dans l'état de l'appareil, pas dans les données ni dans le code de fusion.
  - **On instrumente plutôt que de deviner.** Trois lignes de plus dans « Cet appareil » (`diagnosticAppareilHtml`, js/multiview.js) : **Fusion** (combien de matchs de la grille ont reçu des liens, sur combien — c'est l'étape qu'aucune des trois lignes ne montrait, entre « les liens sont arrivés » et « les cartes sont vides ») ; **Stockage local** (kilo-octets utilisés, et les écritures refusées — `safeStorageSet` avalait `QuotaExceededError` en silence, le cas le plus probable sur un téléphone où la limite est de quelques mégaoctets) ; **Version** (`VERSION_APP`, tenue en phase avec `CACHE_NAME`, plus la présence d'un service worker — un appareil qui tourne encore sur une copie ancienne se reconnaît ainsi).
  - `js/utils.js` : `stockageInfo` (échecs, dernière clé, dernière erreur) et `tailleStockageKo()`. `js/main.js` : `noterFusion` après chaque `setMatches`. `sw.js` → `sports-guide-v11`.
  - Tests : `tests/unit_diagnosticappareil.test.js` (4 groupes, ajouté à `npm test`) — le cas exact de la capture (trois sources vertes, fusion à zéro → la ligne le dit et la marque en échec), le cas normal, une écriture refusée comptée et affichée, et **`VERSION_APP` vérifiée contre `CACHE_NAME` de sw.js** pour que la ligne ne mente jamais. Suite complète verte.
- 2026-09-07 - **« Je reload les streams sont là. Je reload, c'est vide. »** Le symptôme change d'un rechargement à l'AUTRE sur le MÊME appareil : ce n'est donc pas le réseau de l'appareil en général. Vérifié d'abord que ce n'était pas une course dans le code — huit rechargements successifs en local, résultat identique à chaque fois (30 matchs, 18 avec liens), et le cache des liens est bien attendu (`await loadPrefetchedStreams`) AVANT la fusion. Reste la lecture elle-même : **un démarrage à froid n'avait aucun repli.** Les deux essais de `lireCacheServeur` demandent exprès une copie fraîche (`?t=` unique et `cache: 'no-cache'`), ce qui interdit au cache HTTP comme au service worker de répondre ; et `dernierCacheServeur`, la copie gardée d'une lecture précédente, est une variable de MODULE, donc vide à chaque rechargement. Deux téléchargements ratés d'affilée — un réseau qui hésite, une publication GitHub Pages en cours — donnaient zéro lien, alors que le navigateur avait une copie sous la main.
  - `lireCacheServeur` (js/main.js) : troisième essai sur la MÊME adresse, sans `?t=` et sans `cache: 'no-cache'`. Le service worker (réseau d'abord, cache en repli) ou le cache HTTP répond, et l'utilisateur voit des liens un peu plus vieux plutôt que des cartes vides. Le chemin normal reste à un seul aller-retour ; si même le cache est muet, l'échec est signalé comme avant (⚠ sur les cartes, ligne « Liens » dans la page Logs).
  - Tests : `tests/unit_troisiemeessai.test.js` (3 groupes, ajouté à `npm test`) — deux téléchargements ratés puis le cache répond (et l'adresse du troisième essai est bien celle sans paramètre, sans `no-cache`), un seul aller-retour quand le premier aboutit, et les trois en échec restent un échec annoncé. Suite complète verte.
- 2026-09-07 - **« Selon le device, ça voit ou non les scores et les streams. »** Précision de l'utilisateur : ce n'était ni la FIBA ni une ligue, c'était l'APPAREIL. Cause trouvée : le calendrier du jour rangé dans le stockage local n'était jugé que sur sa DATE (`cache.fetchDate === todayStr`). Le premier chargement de la journée écrivait un instantané — la plupart des matchs « à venir », sans score — et tous les chargements suivants le resservaient tel quel **jusqu'à minuit**, sans jamais relire `data/schedule.json`, que le serveur régénère avec les scores et les états réels (le workflow tourne aussi à chaque poussée sur main, donc ~2 fois par heure). Restait le rafraîchissement des scores, qui appelle ESPN **en direct** depuis le navigateur : sur un appareil où cet appel échoue — bloqueur qui filtre `site.api.espn.com`, résolveur d'entreprise, VPN, réseau plus lent que le délai de 8 s — plus rien ne bougeait de la journée. Grille figée au matin, aucun score, onglet Live faux (donc les mauvaises cartes, donc « pas de streams »). Sur un autre appareil, ouvert plus tard, tout s'affichait. Et l'échec d'ESPN était avalé en silence (`catch → null`), si bien que rien ne le disait.
  - `js/api.js` : `CALENDAR_STALE_MS` (10 min, le même seuil que le cache des liens) et `calendrierPerime(cache, todayStr, now)`. Passé ce délai, `getApiFirstMatches` reprend le chemin `data/schedule.json` → ESPN → cache local, au lieu de resservir l'instantané. Chaque écriture du cache porte maintenant `savedAt` (js/api.js ×2, js/main.js ×2) ; **un cache sans `savedAt`, écrit par la version d'avant, est tenu pour périmé** — c'est ce qui remet à jour les appareils déjà figés dès le prochain chargement.
  - **L'échec d'ESPN est compté** (`espnInfo` : tentatives, échecs, dernier succès, dernière erreur) et, quand ni le cache serveur ni ESPN ne répondent, un toast le dit une fois : « Scores indisponibles sur cet appareil ». `window.calendrierInfo` retient d'où vient la grille (`schedule.json`, `espn`, `local`, `local (périmé)`).
  - **Page Logs → « Cet appareil »** (`diagnosticAppareilHtml`, js/multiview.js) : trois lignes comparables d'un appareil à l'autre — calendrier (nombre de matchs, provenance, âge), ESPN (réponses/échecs et la raison), liens `data/streams.json` (nombre de matchs et âge, ou l'erreur). C'est ce qu'il faut regarder quand deux appareils ne montrent pas la même chose.
  - **L'autre moitié, trouvée en instrumentant le test rouge : la grille pouvait S'EFFONDRER.** Quand ESPN est injoignable mais qu'une source annexe répond quand même — le calendrier des galas de catch, lu ailleurs qu'à l'API —, `fetchAndProcessApiMatches` rendait 1 match. Ce n'était pas vide, donc le garde-fou du 6 septembre (« une liste vide est un échec ») laissait passer : ce fragment devenait LE calendrier du jour, la grille tombait de plusieurs dizaines de matchs à un seul, ses liens avec, et le cache local était écrasé par la miette, qui survivait aux passes suivantes. Mesuré : `S.matches` = 1, alors que la liste des liens en avait 199 avec flux. C'est ESPN qui porte le calendrier : si AUCUNE de ses requêtes n'a répondu (compté par `espnInfo` sur la passe), ce qu'on tient est un échec, pas une donnée — le calendrier local est conservé. Le nombre de matchs n'entre pas dans la règle : **un seul match en direct est parfaitement valide**, ce qui est écarté c'est une passe où ESPN n'a rien répondu.
  - Le test « un cache serveur momentanément injoignable… », rouge sur `main` depuis le 7 septembre, passe : c'était ce défaut qu'il attrapait.
  - `tests/test_app_boot.spec.js` : la fiche de match choisit une carte à DEUX blasons (`.prime-logo` ×2) au lieu de la première venue — un événement à un seul nom (course, gala) tombait sur une règle qui ne le concerne pas, selon le programme du jour. **Suite complète verte, 30 tests Playwright compris.**
  - Tests : `tests/unit_calendrierperime.test.js` (5 groupes, ajouté à `npm test`) — frais sous dix minutes, périmé au-delà, périmé sans `savedAt`, périmé pour une autre date / une liste vide / une horloge incohérente, et l'échec ESPN compté avec sa raison. Suite complète : seul reste rouge le test Playwright « un cache serveur momentanément injoignable… », déjà rouge sur `main` (voir PR #551) et dont la cause est dans la fusion, pas ici ; la voie normale garde ses compteurs (« une carte pourvue son compteur » passe).
- 2026-09-07 - **« Sur mon téléphone, y a pas de streams, mais ailleurs oui. Pas de score à date nulle part. »** Relevé sur le cache publié : les sources écrivent les équipes féminines avec un « W » (« Italy W vs China W » chez VIPLeague, « Atlanta Dream W vs Minnesota Lynx W » chez Buffstreams — sous une étiquette « NCAA Men's Basketball » fausse) là où ESPN écrit « China vs Italy » (FIBA World Cup) et « Atlanta Dream » (WNBA). Depuis la règle des catégories (6 septembre), « senior vs F » n'est jamais le même match : les cartes de la FIBA féminine et de la WNBA restaient sans lien, et l'onglet Live du téléphone n'affichait que ces matchs-là.
  - `js/match.js` : `ligueFeminine(league)` (WNBA, PWHL, NWSL, « women », « féminin », « frauen », « ladies », et « FIBA World Cup » — daté : l'édition 2026 est la féminine) ; `categoriesCompatibles` accepte un côté marqué « F » contre un côté sans marqueur DONT LA LIGUE est féminine. Un côté sans marqueur dans une ligue générique reste ambigu (les doublons masculin/féminin d'une même grille, la raison de la règle). Deux côtés sans marqueur s'apparient comme avant.
  - **Scores** : la mise à jour en place (`updateLiveScores`, js/main.js) fonctionne sur la carte en panneau (vérifié hors ligne : 28-10 → 55-41), mais la minute remplaçait « Direct » dans le bandeau. `poserMinuteAuPied` : le bandeau garde « Direct », la minute ou la période va au pied (`.prime-minute`), retirée quand ESPN ne dit rien de plus. Pas de cause trouvée côté code pour « aucun score » : ESPN répond 200 d'ici et le cache du jour porte les scores ; à revérifier avec une capture si ça persiste.
  - **« Où t'as le bleu dans les cartes, mettre les couleurs des équipes en dégradés quand même »** : le panneau entier porte le dégradé des couleurs d'équipes (`--card-grad` posé par js/ui.js), voilé de bleu nuit par la feuille de style pour garder le texte lisible. `sw.js` → `sports-guide-v10`.
  - Tests : `tests/unit_categorie.test.js` (cas 4 révisé, groupe 6 : WNBA et FIBA d'ESPN rejoignent les entrées « W », ligue générique toujours ambiguë, deux côtés nus inchangés, `ligueFeminine`).
- 2026-09-07 - **Liveleagues : la Coupe du monde féminine FIBA a ses liens.** « Nouveau site, qui a les matchs de la FIBA Women actuellement en cours : liveleagues.me/sportshub ». Le site est le moteur de VIPLeague (mêmes identifiants de lignes, même gabarit) mais avec des pages PAR SPORT bien plus fournies que la grille `/watch-now` de vipleague.me (0 match dans le cache du jour).
  - `parseLiveleagues` (js/scrapers.js) : `a[href*="/tag-"]` + `span[content]` (heure de Londres → Est), compétition tirée du premier segment du chemin ; chaînes permanentes (sans heure) ignorées. Sous `/fiba/`, le suffixe « W » des sélections féminines est retiré et la ligue posée à « FIBA World Cup » : ESPN écrit « France vs Nigeria », et `marqueurCategorie` (js/match.js) refuse d'apparier deux catégories différentes — sans cela aucun lien n'atteignait la grille. Vérifié sur la vraie page : 6 des 8 matchs ESPN du jour appariés (les 2 autres étaient finis et plus listés).
  - `js/sources/liveleagues.js` : les diffusions sont en `data-uri` (« Broadcast 1 HD »), invisibles au moteur générique ; extraction + filtre (seules les pages `-live-streaming` du domaine). Les pages se servent sans X-Frame-Options.
  - Enregistrée partout comme Flexfitness : `js/config.js` (URL, miroirs, `SOURCE_VAR_NAMES`, `applySourceUrl`, `fetchRemoteConfig`, `SCRAPERS_CONFIG` avec 25 pages par sport — le baseball renvoie vers mlbbox.me, non listé), `js/main.js`, `scripts/scrape_streams.mjs`, `domains.json`, `js/extractors.js` (`AGGREGATOR_RE`), `js/sources/index.js`, `sw.js` (pré-cache).
  - **Trouvé en route : `sportOfLeague('FIBA WORLD CUP')` rendait `soccer`** (« cup ») : les matchs FIBA cherchaient leurs liens sur les pages de football des sources, jamais sur celles de basket. `\bfiba\b` ajouté à la branche basket (js/db.js).
  - Tests : `tests/unit_scrapers.test.js` (parseLiveleagues, page de match), `tests/unit_adaptateurs.test.js` (registre, extraction, filtre, 5 fichiers).
- 2026-09-07 - **Cartes en panneau : « J'aime ce look, qui ressemble à celui qu'on a déjà, mais plus clair. Possible de l'adapter en gardant un gradient ? »** (capture : carte bleu nuit, bandeau LIVE rose, blasons ronds, titre, lignes équipe/score, sport en pied).
  - `js/ui.js` (grille de cartes) : nouveau balisage — `.prime-head` (bandeau d'état : Direct / heure / Fin / Fin ?, compteur de flux à droite), `.prime-thumbnail` (le dégradé des couleurs d'équipes reste, voilé de bleu nuit ; blasons ronds cerclés), `.prime-title` (la ligue, plus de pastille sur l'image), `.prime-info` (lignes équipe/score), `.prime-foot` (sport par `libelleSport`, js/db.js, et minute ou période du direct). Les classes `.prime-*` sont conservées.
  - `styles.css` : bloc carte réécrit (variables `--card-panel*`), bandeau du direct en dégradé rouge → rose, affiche verticale (`body.cards-poster`) refaite sur le même empilement au lieu d'un texte superposé à l'image. `styles-legacy.css` : règles sobres pour les nouveaux blocs. `sw.js` → `sports-guide-v9`.
  - Test « sur mobile, les affiches tiennent en grille… » adapté : c'est la carte qui est en portrait, la vignette est un bandeau. Vérifié en capture (bureau et téléphone). Deux tests Playwright (« la fiche de match a une seule croix… », « un cache serveur momentanément injoignable… ») échouent déjà sur `main` sans ces changements, avec les données du jour : pas touchés ici.
- 2026-09-07 - **« J'ai voulu ajouter un stream dans un match et là, la page me redirect non stop vers le stream sur le site externe. »** Sans attribut `sandbox` sur les iframes (retiré le 5 septembre à la demande de l'utilisateur), une page encadrée peut naviguer la fenêtre entière (`top.location`) ; le Multivision restauré au chargement (`mv_state`) reposait la tuile, et la boucle repartait.
  - `js/multiview.js` : la tuile note `_posedAt` quand son iframe est posée ; sur `pagehide`, les tuiles posées depuis moins de 15 s sont notées (`mv_sortie_forcee`). À la restauration, ces tuiles sont marquées `_sortieForcee` et ne sont PAS rechargées : un avertissement dans la tuile propose « ↗ Ouvrir sur le site » ou « Charger quand même » (`chargerQuandMeme`). Un rechargement volontaire dans les 15 s qui suivent la pose d'une tuile coûte un clic ; une boucle, elle, n'a plus de fin.
  - Tests : `tests/unit_sortieforcee.test.js` (`suspectsDeSortie`, `marquerSortiesForcees`, `chargerQuandMeme`), ajouté à `npm test`.
- 2026-09-07 - **Fin présumée : un match sans nouvelle d'ESPN ne reste plus « DIRECT » des heures.** « Des matchs qui n'arrêtent pas même si finaux sur ESPN, c'est normal ? » Non : un match ne passait à « Fin » que sur l'ordre d'ESPN, et quand les requêtes vers ESPN échouent (réseau, onglet gelé) rien ne le disait — la carte restait DIRECT jusqu'à quatre heures après le coup d'envoi. Précision demandée : « ça continue sauf si indication de ESPN de extra innings ou périodes ».
  - **`js/finpresumee.js`** (module sans import) : `finPresumee(m, ecouleMin, dureeMin, now)`. ESPN qui parle a raison : une mise à jour de score reçue il y a moins de 12 min désarme la règle, quelle que soit la durée. Sans nouvelle, passé la durée normale du sport + 45 min, le match est présumé fini. Si la dernière nouvelle disait déjà « prolongation » (`enProlongation` : période au-delà du règlement — 9 manches, 3 tiers, 4 quarts, 2 mi-temps — ou libellé OT / extra / shootout / « Top 11th »), la marge passe à 120 min. `raisonFinPresumee` fournit l'infobulle.
  - `js/api.js` : `period` et `detail` (« Top 10th », « OT », « End of 3rd ») lus dans ESPN et recopiés par `applyScoreUpdates`, qui pose aussi `_scoreAt` (horodatage de la dernière nouvelle pour CE match). `js/config.js` : enveloppe `finPresumee(m, now)` (durée par `durationMinutes` ou `getLeagueDuration`) et `isLiveNow` la respecte — le match quitte le Live.
  - Rendu : « Fin ? » (ambre, infobulle explicative) sur les cartes du Live/Guide, la grille temporelle et la bannière de la fiche ; `updateLiveScores` (js/main.js) le pose aussi lors des mises à jour. `reevaluerFinsPresumees` tourne chaque minute — c'est précisément quand ESPN se tait que rien d'autre ne redessine — et reconstruit le Live si l'ensemble a changé.
  - `sw.js` → `sports-guide-v8`. Tests : `tests/unit_finpresumee.test.js` (4 groupes, ajouté à `npm test`) et « sans nouvelle d'ESPN, un match trop long passe à « Fin ? » et quitte le Live, sauf prolongation connue » (Playwright).
- 2026-09-07 - **Fiche de match : logos et couleurs des équipes dans le haut.** Demande : « Intégrer les logos et couleurs des équipes dans le haut. » L'en-tête portait une ligne de texte tronquée (« MLB Chicago White Sox — Min… ») et le tableau d'affichage vivait plus bas, dans le corps.
  - `openMod` (js/ui.js) : l'en-tête `.mhd` devient la **bannière** — fond au dégradé des couleurs des équipes (le même calcul que la carte, `cardBg`), les deux blasons, les noms avec l'étoile de favori, le score et l'état, la ligue en pastille. Classe `has-banner` sur `.mhd`, bannière `.fiche-banner` dans `#mname`.
  - Le corps ne répète plus vignette, noms et score : le panneau `.fiche-complements` ne garde que buteurs, classement, statistiques et bouton ESPN, et n'apparaît (classe `has-content`, observateur de mutations) que quand l'un d'eux a quelque chose — sinon la liste des flux prend toute la largeur (`#modal-left-col:has(...)`, styles.css).
  - Variables devenues inutiles retirées de `openMod` (`lgBadge`, `logosHtml`, `teamsHtml`, `streamsBadgePrime`). Feuille classique : mêmes règles ajoutées à `styles-legacy.css`.
  - Test « la fiche de match a une seule croix… » étendu : bannière présente, deux blasons, fond aux couleurs des équipes, plus de vignette dans le corps.
- 2026-09-07 - **Feuille de match sur téléphone : « mal placé en haut et difficile de sortir de la carte ».** Sa hauteur maximale était en `100vh` — sur téléphone, la plus grande hauteur d'écran (barre d'adresse repliée), plus haute que la zone visible : la feuille débordait par le haut en emportant sa croix, et la barre du bas (z 1000, sortie du contexte de l'en-tête le 6 septembre) recouvrait ses derniers flux.
  - `styles.css` (bloc mobile) : hauteur en pourcentage du fond fixé (`calc(100% - 44px)` de `.mbg`, qui suit la zone visible), `.mbg` au-dessus de la barre du bas (z 1100), bande de fond conservée en haut (toucher = fermer).
  - `index.html` : gros bouton « ✕ Fermer » au bas de la feuille (`.sheet-close`, mobile seulement), toujours visible, hors défilement.
  - Test « sur mobile, les onglets forment une barre au bas de l'écran et la fiche monte du bas » étendu : la feuille ne déborde pas par le haut, la croix est dans l'écran, le bouton Fermer est visible, n'est pas sous la barre du bas, et ferme.
- 2026-09-07 - **« Ça reste stuck là » : l'écran d'attente sur « Recherche de streams… ».** Capture sur téléphone à 20 h 19, juste après la publication. Sans cache serveur utilisable, le premier chargement lisait chaque source par chaque proxy CORS (direct 5 s, puis trois proxys à 8 s chacun, par page) AVANT d'afficher quoi que ce soit : une minute et plus, écran vide, sans dire pourquoi.
  - **Affichage immédiat** (js/main.js, chemin sans cache) : le calendrier est déjà là, on l'affiche tout de suite avec ce qu'on a de liens (⚠ ou 🔎 sur les cartes) ; la lecture des sources continue derrière et la fin de la passe redessine la grille.
  - **La cause est dite** : l'écran d'attente affiche « cache serveur injoignable : <raison> », et un toast le répète au moment de l'affichage. La prochaine capture dira ce que le téléphone n'arrive pas à lire.
  - Test : « cache serveur injoignable et proxys muets : la grille s'affiche sans attendre la recherche » (les proxys ne répondent jamais ; la grille doit apparaître en moins de 15 s, écran d'attente retiré, badges ⚠, raison affichée).
- 2026-09-07 - **« Pas de liens pour ce match ? » : le fichier des liens n'était pas arrivé, et l'application n'en disait rien.** Capture sur téléphone (réseau cellulaire) : toutes les cartes portaient 🔎 alors que le cache publié avait 44 liens pour le match montré, et qu'un chargement frais sur écran de téléphone en affichait 44. Le fichier `data/streams.json` (750 Ko) n'était pas arrivé — téléchargement coupé, ou réponse d'erreur pendant une publication GitHub Pages — et l'échec était traité comme « aucun lien nulle part ».
  - **Deux essais, et le dernier cache lu est gardé** (`lireCacheServeur`, `appliquerCacheServeur`, js/main.js) : un second essai après 1,5 s ; si les deux échouent, le dernier cache lu avec succès est ré-appliqué depuis sa copie texte (des objets neufs — réutiliser ceux déjà fusionnés dans la grille la vidait), `prefetchedStreamsError` retient l'échec et le cache est marqué « à relire » pour la passe suivante.
  - **Retour au premier plan** (js/main.js) : si le cache a plus de dix minutes ou n'a pas pu être lu, une passe d'arrière-plan repart dès que la page redevient visible — les scores l'étaient déjà (js/api.js), les liens attendaient la minuterie de cinq minutes, gelée en arrière-plan sur téléphone.
  - **Badge ⚠ sur les cartes** (js/ui.js, `cardRetryLinks` js/links.js) quand le cache serveur n'a pas pu être lu, distinct du 🔎 (« aucun lien connu », qui lance une recherche par proxys). Un toucher relit le cache et redessine la grille.
  - **Trouvé en écrivant le test : le rafraîchissement des scores écrasait le calendrier du jour par une liste vide** quand toutes les requêtes ESPN échouaient (`fetchAndProcessApiMatches`, js/api.js) ; la passe suivante lisait ce vide et la grille disparaissait jusqu'à « Réessayer ». Un résultat vide n'est plus écrit, et `getApiFirstMatches` retombe sur le calendrier local si l'API ne rend rien (`apiOuCacheLocal`).
  - Tests (`tests/test_app_boot.spec.js`, service worker bloqué pour que l'interception fasse foi) : « cache serveur injoignable au démarrage : deux essais, badge ⚠ plutôt que 🔎, et un toucher rétablit les liens » ; « un cache serveur momentanément injoignable ne vide pas la liste, ni la grille, ni le calendrier local, et le retour au premier plan la relit ».
- 2026-09-06 - **Haut de page nu, menus du lecteur par-dessus les tuiles, lecture immédiate, repli vers la page du site.** Demande : « Enlever logo et favicon en haut à gauche, faire que le dropdown des options soit par-dessus le multiview, faire que les vidéos dans le multiview se lancent directement, bouton pour fallback au player dans la page originale. Enlever tous les éléments dans le haut de la page (recherche, toggles de ligue et cie), garder UI et UX le plus clean possible, compréhensible pour un boomer. Nickel sur tous les écrans. »
  - **Haut de page** (`index.html`, `styles.css`) : la marque (icône + « Guide des Sports ») et toute la barre d'outils (recherche, ↻, liens manquants, date, pastilles de ligues) sont retirées ; il ne reste que les onglets, centrés et plus gros. Le code qui les servait reste, gardé, pour l'interface classique. Sur mobile l'en-tête n'a plus de hauteur et ne crée plus de contexte d'empilement — c'est lui qui faisait passer la barre du bas et le menu « Plus » sous le lecteur.
  - **Menus par-dessus** (`js/mv-menu.js`, nouveau) : les menus déroulants vivaient DANS la tuile ou la barre, rognés par `overflow: hidden` et recouverts par la tuile voisine. Un seul menu flottant, en position fixe sur le document (ou l'élément en plein écran), à l'index maximal, ramené dans l'écran ; se ferme au clic ailleurs, à Échap, au défilement, et quand la fenêtre perd le focus (clic dans une iframe). Barre du lecteur ramenée à Ajouter / Disposition / Plein écran / Plus ; observateur de repli de la barre retiré.
  - **Repli vers le site** : bouton « ↗ Site » visible sur chaque tuile (`ouvrirPageOriginale`) + entrée de menu. Recharger passe par le chemin normal de pose (`rechargerTuile`).
  - **Lecture immédiate** (`multiview-cleaner.user.js` 1.8) : après nettoyage, `play()` sur la vidéo — refusé avec le son, relancé muet — et un clic, un seul, sur le gros bouton de lecture des lecteurs connus ; relancé quand l'application rend le son à la tuile.
  - **Tous les écrans** : géométrie du lecteur par classe CSS (`.mv-full`) au lieu de `cssText` calculé ; en portrait les tuiles s'empilent (`dispositionEffective`) sans écraser le choix ; en-tête de tuile en conteneur (libellés, poignées, numéro disparaissent quand la tuile rétrécit) ; tactile sans poignées ; onglet « Lecteur » allumé quand le lecteur s'ouvre (`syncNavState` exporté). Vérifié par captures à 1366, 820 (tactile, portrait) et 390 px : Live, Guide, lecteur à 2 et 4 tuiles, menus, PiP.
  - `styles-legacy.css` : appendice complété (coquille du lecteur, barre, tuiles, menu) pour que l'interface classique, qui partage le JavaScript, reste entière. `sw.js` → `sports-guide-v7`, pré-cache complété. Tests : 3 scénarios de barre d'outils remplacés par 2 (haut de page nu ; menus par-dessus) + 1 dans `test_cleaner` (lancement sans clic). 26/26 Playwright, unitaires verts.
  - **Non fait, volontairement** : la navigation par date et la recherche n'ont pas été replacées ailleurs (demande : tout enlever du haut) ; elles restent dans l'interface classique. `legacy.html` n'est pas touché.
- 2026-09-06 - **Scores en direct : le rafraîchissement ESPN se voit partout, et un match terminé quitte le Live.** « Scores en direct brisés ? Aussi, live doit disparaître si match fini selon ESPN. » Deux défauts, tous deux dans le chemin `backgroundUpdateGuide` (js/api.js, toutes les cinq minutes et au retour au premier plan).
  - **Les objets restaient vieux.** ESPN rendait des objets NEUFS et seul le DOM était patché : `S.matches` — ce que lisent la fiche, le filtre du Live et toute reconstruction de la grille — gardait statut, score et minute d'il y a jusqu'à cinq minutes. `applyScoreUpdates` (js/main.js) reporte d'abord les données par identifiant dans `S.matches`, puis met à jour l'affichage.
  - **Le Guide ne bougeait jamais.** Le patch ne cherchait que `.status-minute` et `.prime-score`, absents des blocs de la grille temporelle : un bloc pouvait afficher « 19:05 » une heure après le coup d'envoi, sans score. `timelineBadgeHtml` (js/ui.js) produit la pastille heure/minute/score et sert aux deux — construction et mise à jour.
  - **Un match terminé restait dans le Live** avec la mention « Fin » jusqu'au prochain changement d'onglet, puisque rien ne reconstruisait la vue. `belongsToLive` (js/ui.js) dit la règle d'appartenance ; si l'ensemble des matchs qui devraient être dans le Live diffère de celui des cartes rendues (un terminé à retirer, un nouveau à montrer), la vue est reconstruite en conservant le défilement. La fiche ouverte vit hors de `#marea` et garde son objet, désormais à jour.
  - Test : « un match terminé selon ESPN quitte le Live, et le Guide suit le score » (rafraîchissement simulé par `applyScoreUpdates`).
- 2026-09-06 - **Refonte UI/UX : plus rapide, plus simple, et rien qui casse.** Demande : « refaire ui et ux pour améliorer rapidité, utilisation facile, fonctionnalités qui ne brisent pas ». État des lieux par captures Playwright (bureau 1366 px, mobile 390 px) avant de toucher quoi que ce soit ; l'ancienne présentation reste livrée à côté (voir plus bas).
  - **Trois fonctions inatteignables, rendues.** `#sport-filters-container { display: none !important; }` traînait deux fois dans `styles.css` : la barre d'outils entière (navigation par date, rafraîchissement des scores, « 🔎 Liens manquants ») était masquée alors que le code, le README et les tests la décrivaient. Les pastilles de ligues, elles, n'étaient construites que dans la branche « scraping en direct » de `loadAllRun` : dès que le cache serveur suffisait (le cas normal), la rangée restait vide — `renderSportChips` (js/ui.js) les rend maintenant à chaque construction de la grille. Et la recherche (`S.searchQuery`, déjà appliquée par `buildEPG`) n'avait aucun champ : `#search-input` + `setSearchQuery` (js/utils.js), débounce 160 ms, état vide qui propose d'effacer.
  - **Deux croix dans la fiche de match.** `document.querySelector('.mhd')` attrapait l'en-tête de l'Investigator (première `.mhd` du document), jamais celui de la fiche ; `openMod` injectait donc une seconde croix par-dessus la colonne des flux. L'en-tête de la fiche sert désormais d'en-tête (titre, ligue, croix unique), Échap ferme. Sur mobile la fiche est une feuille qui monte du bas, pleine largeur.
  - **Coquille (`index.html`)** : en-tête (marque, onglets avec icônes, menu « Plus »), barre d'outils Live/Guide (recherche, ↻, liens manquants, date), pages Favoris/Options/Logs/Script en classes (`.page`, `.panel`, `.field`) au lieu de styles en ligne. Options : l'apparence d'abord, « Réseau & proxys » et « Outils » repliés dans des `<details>`. Tous les identifiants et gestionnaires `onclick` sont conservés.
  - **Feuille de style réécrite (`styles.css`, 980 → ~600 lignes)** : jetons, une section par surface, et surtout **plus aucun `backdrop-filter` sur un élément répété** (chaque `.btn`, badge de carte, pastille de flux en portait un — un rendu hors écran par élément, première cause de saccades au défilement). `transition: all` remplacé par des propriétés nommées, `content-visibility: auto` sur les cartes, `loading="lazy" decoding="async"` sur les logos, `prefers-reduced-motion`. Retirés : le bloc `:root` dupliqué dans une media query et ~80 lignes de CSS orphelin (un `*/` égaré à la ligne 552 avalait tout le bloc responsive du « game mode »). Cellules collantes de la grille temporelle rendues opaques (les blocs transparaissaient à travers).
  - **Navigation** : `applyFilter` (js/utils.js) ne pose plus couleur/bordure/fond en `style` sur chaque bouton, une classe suffit (`aria-current` en prime) ; `showPage` remplace quatre copies du même bloc « masquer les autres pages » ; `body[data-view]` porte la vue active pour la feuille de style. Mobile : onglets en barre fixe au bas de l'écran (cibles de 44 px), l'en-tête ne porte plus de `backdrop-filter` — il faisait bloc conteneur de la barre fixée, qui se retrouvait collée en haut.
  - **Multivision** : état vide avec un vrai bouton « Ajouter un match » ; bloc clavier dupliqué à l'identique retiré ; **ajustement du contenu par tuile** (demande « fit content dans multiview ») — bouton ⤢ dans l'en-tête de chaque tuile et dans la barre pour toutes : étiré (comportement d'origine) → ajusté (16:9 entier, centré) → rempli (16:9 couvrant, bords rognés). Calcul par unités de conteneur (`cqw`/`cqh`), rien à observer ; retenu par tuile dans `mv_state`.
  - **Interface classique conservée** (demande « possible pour l'instant d'avoir un toggle pour le tester ? ») : `legacy.html` + `styles-legacy.css` sont l'ancienne coquille telle quelle, partageant tout le JavaScript (un appendice de la feuille classique couvre les classes que le code partagé pose désormais). Options → « Interface classique », menu « Plus » → « ↩ Interface classique », et depuis la classique « ✨ Nouvelle interface ». Le choix (`ui_legacy`) est retenu ; `index.html` redirige au démarrage. Le service worker précache les deux (`sports-guide-v6`) ; le workflow Android copie les deux.
  - **Ce qui n'a pas bougé, volontairement** : le dégradé aux couleurs des équipes sur les cartes (« garde les aspects couleurs des équipes »), la logique de rendu des cartes, la grille temporelle, l'affiche verticale mobile, les préférences de thème (`initPrefs` pose les mêmes variables CSS).
  - **Tests** : sept scénarios ajoutés à `tests/test_app_boot.spec.js` (barre d'outils atteignable, pastilles rendues depuis le cache et filtrant, recherche + état vide, fiche à croix unique + Échap, barre du bas et feuille mobile, interface classique qui démarre et redirection de la préférence). 17/17 verts, 35 fichiers unitaires verts.
- 2026-09-06 - **Le débit réel dans la liste des sources, mesuré et non annoncé.** « Moyen d'avoir le débit dans la liste des feeds ? » La liste montrait la qualité ANNONCÉE par la source — « HD », « 1080p », parfois un « 3500 kbps » recopié d'un libellé. C'est du déclaratif, et souvent faux : des liens marqués « HD » ne jouaient pas du tout, et deux « SD » d'un même match n'avaient rien à voir l'un avec l'autre.
  - Le script utilisateur est dans le cadre : il rapporte désormais deux choses qu'il ne faut pas confondre. La **définition** vient de l'élément `<video>` (`videoWidth`/`videoHeight`), toujours exacte. Le **débit** vient des octets réellement transférés (chronologie des ressources, segments `.ts`/`.m4s`/`.mp4`, fenêtre de 10 s) — et il n'est PAS toujours mesurable : `transferSize` d'une ressource d'origine croisée vaut 0 tant que le serveur n'envoie pas `Timing-Allow-Origin`, ce que beaucoup de CDN omettent.
  - Règle d'affichage, la même que pour les faux flux : on montre ce qui a été mesuré, et rien d'autre. Une fenêtre sans octets n'efface pas la mesure connue (sinon la liste clignoterait), et une mesure entièrement vide n'est pas inscrite.
  - `js/debit.js` (module sans import) tient le calcul, la mise en forme et le registre par flux (3 h). La tuile affiche la mesure dans sa pastille, la liste des sources dans une puce verte « 1080p · 5,2 Mb/s ». Script utilisateur en 1.7. Tests `unit_debit` et `test_cleaner`.
- 2026-09-06 - **League of Legends : les matchs étaient là, aucun ne pouvait jouer.** « Pour league : lolesports.com. » L'application interroge l'API officielle depuis longtemps — `getSchedule` remplit la grille, `getLive` donne les flux — et les matchs apparaissaient bien : relevé ce jour-là, **sept rencontres** LCS, LEC, LCK, LPL et PCS, dont un LCS en direct (Shopify Rebellion – FlyQuest). Mais **zéro flux jouable**, à cause de la seule forme des adresses construites : `https://youtube.com/watch?v=<id>` (une page de visionnement) et `https://twitch.tv/<chaîne>` (une page de chaîne).
  - Mesuré aux en-têtes sur ce match : `twitch.tv/lcs` répond « X-Frame-Options: SAMEORIGIN » — Twitch REFUSE l'encadrement ; `player.twitch.tv/?channel=lcs&parent=<notre hôte>` répond « frame-ancestors https://ouellettejeanphilippe-source.github.io », soit une autorisation explicite ; `youtube.com/watch?v=…` répond 429 et n'est de toute façon pas encadrable ; `youtube.com/embed/<id>` répond 200 sans restriction de cadre.
  - `js/esports.js` (module sans import) construit la forme que le fournisseur accepte d'encadrer. Twitch exige que `parent` soit exactement l'hôte de la page — c'est sa protection contre le vol de flux, et l'oublier fait échouer le lecteur en silence ; l'hôte vient de `location`, avec repli sur celui de production pour une coquille WebView ou un fichier local. Un fournisseur qu'on ne sait pas encadrer ne produit aucun lien, plutôt qu'un lien qui afficherait l'écran de refus du navigateur dans la tuile.
  - `js/api.js` : les deux blocs qui construisaient ces adresses à la main (dupliqués mot pour mot) sont remplacés par `liensDunEvenementEsports`. Test `unit_esports`, sur la charge réelle de `getEventDetails` : quatre flux distincts, doublon et fournisseur inconnu écartés.
- 2026-09-06 - **Les zones de régie, reconnues à la forme de leur adresse.** Suite du retrait du décor. Cette règle-là juge sur les DONNÉES — une adresse qui sert à plusieurs sports n'est pas un flux de match — et a donc besoin de voir l'adresse se répandre. Vérifié sur le cache de 18 h 52, juste après sa mise en service : elle avait bien retiré les gros fournisseurs (le bandeau de footybite sur 74 matchs, `hai8g.com/4/8553101` sur 35), mais douze liens du même genre restaient, vus une ou deux fois chacun dans l'heure — sans preuve, donc épargnés à raison.
  - Ces adresses-là se reconnaissent sans données : un répertoire numérique court suivi d'un identifiant numérique long, et **pas un seul mot** dans le chemin (`hai8g.com/4/11690714`, `omg10.com/4/9020608/`). C'est le format de zone des régies. Un lecteur porte toujours un mot — embed, stream, live, player, channel, watch, ou le nom du match.
  - Vérifié AVANT d'écrire la règle, sur les **2 479 adresses distinctes** de trois relevés du cache : elle reconnaît exactement les cinq zones de régie observées et **aucun** vrai lecteur ; il n'existe dans ces données aucun lecteur à chemin purement numérique. Ajoutée à `isJunkStreamPath` (js/scrapers.js) plutôt qu'en filtre séparé — c'est déjà la porte par laquelle passe chaque lien, côté serveur comme côté navigateur.
  - Les dix pages `shd247.world/live-soccer-streaming-NN` restantes ne sont PAS touchées : elles portent des mots, n'apparaissent que dans un seul sport, et pourraient être de vraies chaînes. Le doute leur profite ; la règle des sports les attrapera d'elle-même si elles se répandent.
- 2026-09-06 - **Le décor et la publicité déguisés en lecteurs : 138 liens qui n'en étaient pas.** « Fais une règle qui les pogne tous sans pogner de faux streams. » Mesuré sur le cache de 17 h 44 (410 matchs, 1 876 liens) : la médiane des matchs pourvus était de **UN seul lien**, et pour beaucoup ce lien unique était le même pour tout le monde. Quatre adresses portaient à elles seules 132 liens — `dcbbwymp1bhlf.cloudfront.net/?wbbcd=1244494` sur **74 matchs** (le bandeau de footybite), `hai8g.com/4/8553101` sur 35, `omg10.com/4/9020608//` sur 16, `hai8g.com/4/11695852` sur 7. Ouvrir une de ces fiches affichait « 1 flux » et menait à une page de publicité.
  - Le partage seul ne prouve rien : de vraies chaînes sont légitimement partagées — « NHL Network » sert cinq rencontres de hockey, le flux Sky Sports F1 les séances d'un même week-end. Ce qui trahit le décor, c'est de traverser les **sports** : le bandeau de footybite apparaît en soccer, cricket, tennis, MLB et catch ; une page `shd247.world/live-go-streaming-21.html` sert à la fois un match de Süper Lig et un Padres–Yankees.
  - `adressesNonSpecifiques` / `retirerLiensDeDecor` (js/match.js), appliqués dans le scraper une fois toutes les sources fusionnées — la règle a besoin de voir l'ensemble des matchs, ce qu'aucune page ne permet seule. Une ligue non reconnue (`other`) ne suffit jamais à condamner une adresse, et les liens « Page du match », déjà marqués, ne sont pas concernés.
  - Effet mesuré sur le cache réel : **7 adresses écartées, 138 liens retirés, 47 fiches** qui affichaient un faux flux montrent désormais leur vrai état. Vérifié qu'aucune des neuf chaînes légitimes partagées n'est touchée. Test `unit_decor`.
- 2026-09-06 - **Une grille servie sous « introuvable » n'est plus jetée, et chaque hôte est rattaché à sa source.** Deux défauts trouvés en sondant les domaines frères de Sportsurge.
  - **La grille perdue sur son code HTTP.** `soccersurge.io`, bras soccer de Sportsurge, est le seul de ses domaines joignable depuis un centre de données — `v2.sportsurge.net` y rend 403, avec 5 Ko de page d'erreur. Or soccersurge répond **404 sur toutes ses adresses**, accueil compris, tout en servant 181 Ko contenant 53 liens de match ; `fetchPage` jetait cette réponse sur son seul code, avant de regarder le corps. Vérifié après correction : cette grille donne **55 matchs** (La Liga, Serie A, Belgian Pro League, Peruvian Liga 1…).
  - **Le piège, mesuré aussi** : ce site sert la MÊME grille sur n'importe quel chemin, y compris inventé (`/nexiste-pas-0000` rend les mêmes 53 liens). Accepter ce corps pour une PAGE DE MATCH y injecterait 53 liens étrangers attribués au mauvais match — le défaut même qu'on a passé la semaine à démêler. D'où la frontière : `statutAcceptable` (js/fetcher.js) n'accepte 404 et 410 que si l'appelant le demande, et seule la découverte des listes le demande. Un 403 ou un 429 reste un refus, sans quoi la bascule vers un miroir vivant ne se ferait plus.
  - **Le relevé par source était faux.** Les liens trouvés sur `soccersurge.io` étaient comptés sous « footybite », la source qui avait DÉCOUVERT le match, faute de reconnaître le domaine : `sourceIdPourHote` (js/config.js) rattache désormais un hôte à sa source en tenant compte de ses miroirs. Aucun lien n'était perdu, mais c'est sur ce relevé qu'on décide quelle source est en panne.
  - `soccersurge.io` ajouté en dernier miroir de Sportsurge (sa grille est surtout du football, là où sportsurge.net couvre tous les sports). Test `unit_soft404`.
- 2026-09-06 - **Les tuiles du Multivision se relisent aussi, tant qu'on regarde.** Suite de la mise à jour à même la page. La fiche ouverte se relit chaque minute depuis le changement précédent ; mais quand on ferme la fiche et qu'on ne garde que le Multivision — le cas normal quand on regarde vraiment — plus rien ne relisait, et la tuile restait avec la liste de sources qu'elle avait au moment où on l'a posée. Or c'est précisément quand un flux lâche qu'il faut avoir où aller : le bouton « source suivante » ne vaut que par la longueur de sa liste.
  - `doitRafraichir(m, intervalle, …)` (js/scrapers.js) porte désormais la règle commune ; `doitRafraichirFiche` (1 min) et `doitRafraichirTuile` (3 min) n'en sont que deux cadences. Plus lent pour les tuiles : on suit jusqu'à quatre matchs à la fois, et une tuile qui joue n'a pas besoin d'une liste fraîche à la minute.
  - Un seul minuteur, armé à l'ajout d'une tuile ET à la restauration d'une session (des tuiles restaurées ne passent pas par `addToMultivision` : sans cela, une session reprise ne relisait plus jamais), coupé quand il ne reste plus de tuile. Rien n'est redessiné : seule la pastille « source k/n » est mise à jour, pour ne pas interrompre une vidéo en cours.
- 2026-09-06 - **La fiche ouverte se met à jour toute seule : les flux arrivent au fil du match.** « Ok pour le cache de liens et de matchs, mais meilleure mise à jour des streams à même la page. » Ouvrir une fiche déclenchait UNE relecture, puis plus rien : la fenêtre restait figée sur ce qu'elle avait trouvé à la seconde de son ouverture.
  - Ce n'est pas théorique. Mesuré le même jour sur les pages réelles de Footybite : Everton–Manchester United, bien engagé, annonçait « Available Streams (55) » ; Arsenal–Chelsea, qui venait de commencer, en annonçait 5. Les liens arrivent au fil du match, et le match qu'on regarde est justement celui dont la liste bouge le plus.
  - `doitRafraichirFiche` (js/scrapers.js) : tant que la fiche reste ouverte, sa page est relue chaque minute. Trois conditions, pour ne pas marteler — le pont du script est là (sans lui aucun transport ne passe), le match n'est pas terminé, et la dernière lecture date de plus d'une minute. Test `unit_fiche_continue`.
  - Côté fenêtre (js/ui.js) : un seul minuteur pour toute l'application, coupé à la fermeture de la fiche et avant tout réarmement ; une relecture déjà en cours n'en déclenche pas une seconde. La liste ne clignote pas — le scrape fusionne, et la fenêtre n'est redessinée que si le nombre de flux a réellement augmenté.
- 2026-09-06 - **Les quatre domaines à forme propre sont migrés : plus aucune branche par hôte dans le module central.** Suite de « chaque domaine doit être différent dans le système de fetch and parse ». `extractStreamLinks` ne contient plus une seule ligne `pageHost.indexOf('…')`.
  - `js/sources/onhockey.js` : ses matchs ne pointent pas vers une page par match mais tous vers `schedule_table.php`, sa grille unique — lire « la page du match » revient donc à relire la grille et à y retrouver la ligne. L'adaptateur reçoit pour cela le parseur de liste de son domaine et l'appariement dans son contexte (`ctx.aides.parseListe`, `ctx.aides.memeMatch`), plutôt que de les importer.
  - `js/sources/streameast.js` : charge Next.js, forme propre. `js/sources/vipleague.js` : ce domaine ne sert AUCUN lecteur dans son HTML (tout en JavaScript, derrière un jeton CSRF) et n'apporte donc pas un extracteur mais un **filtre** sur la récolte générique — d'où le second point d'entrée du contrat, `filtrerLiens`.
  - Règle d'import précisée et vérifiée par le test : un adaptateur ne doit importer ni `js/scrapers.js`, ni `js/utils.js`, ni `js/config.js`, car les deux derniers remontent vers le premier (mesuré : utils importe scrapers, config importe utils). Les modules feuilles (db, match, teams, extractors) sont sûrs.
  - Vérifié sur les pages réelles avant/après : Everton–Manchester United 53 liens, Arsenal–Chelsea 1, grille OnHockey 51 — identiques.
- 2026-09-06 - **Un fichier par domaine : `js/sources/`.** « Chaque domaine doit donc être différent dans le système de fetch and parse. » Les agrégateurs ne se ressemblent pas — l'un rend ses flux dans une charge Next.js, l'autre dans une iframe, un troisième dans une API JSON — et la chaîne qui mène à la vidéo diffère aussi : accueil → page du match → lecteur chez l'un, accueil → liste → page → lecteur chez l'autre. Le parse des LISTES était déjà par domaine (15 fonctions `parse*`) ; l'extraction des flux d'une page, elle, tenait dans UNE fonction de 582 lignes avec quatre branches par hôte à l'intérieur, plus un filtre commun — le nid à régressions qu'on a démêlé plusieurs fois cette semaine.
  - `js/sources/index.js` tient le registre et le contrat : `hotes` (fragments reconnus) et `extraireLiens(ctx)`. Le contexte est PASSÉ à l'adaptateur (page, document, match, aides) : un adaptateur n'importe rien du module central, sinon le graphe deviendrait circulaire et chaque adaptateur retirerait tout le reste derrière lui. `unit_adaptateurs` verrouille cette règle en lisant les imports de chaque fichier.
  - `js/sources/footybite.js` : première migration, code déplacé tel quel. Vérifié sur les pages réelles avant/après — Everton–Manchester United 53 liens, Arsenal–Chelsea 1, identiques.
  - Un domaine sans adaptateur ne change pas de comportement : branches restantes puis moteur générique (`js/extractors.js`), comme avant. Les autres domaines migreront un par un, chacun avec sa mesure.
- 2026-09-06 - **Coquilles tolérées, catégories jamais confondues : le match féminin n'absorbe plus le match masculin.** « Le système de noms d'équipes doit inclure qu'il y a généralement des typos, mais que c'est reconnaissable quand même. » Mesuré avant de coder, sur le cache du jour (748 noms distincts, 29 paires à un ou deux caractères d'écart) : **la similarité ne peut pas séparer une coquille d'une équipe distincte**, parce que les deux vivent dans les mêmes chiffres — « arsenal / arsenl » (vraie coquille) donne 0,857, tandis que « newyorkjets / newyorkmets » donne 0,909 et « nflnetwork / nhlnetwork » 0,900. Tout seuil qui accepte la première accepte les deux autres.
  - Ce qui les sépare est le contexte, pas les lettres, et il fait déjà son travail : vérifié, « Arkansas State vs Iowa State » ne s'apparie pas à « Kansas State vs Army », ni l'Inter de Milan à l'Internacional de Porto Alegre. Les coquilles passent déjà, d'un côté comme des deux (« Tanpa Bay Lightning vs Florida Panthrs »). Rien à assouplir de ce côté.
  - Restait le cas que le contexte ne tranche pas, parce que tout y concorde sauf une lettre : l'équipe féminine et l'équipe masculine du même club. Relevé sur le cache : Atlanta Dream W, Bayern Munich W, Connecticut Sun W, Dallas Wings W, Los Angeles Sparks W, Portland Fire W, Seattle Storm W. L'appariement réussissait, et `mergeMatches` ne gardant qu'une entrée, **un des deux matchs disparaissait de la grille avec ses liens**.
  - `marqueurCategorie` (js/match.js) lit un marqueur de catégorie en fin de nom — féminin (W, Women, Frauen, Femenino…), âge (U15 à U23), réserve (II, B, Academy) — et `debugMatchPair` refuse d'apparier deux catégories différentes, avant toute comparaison de noms. Deux libellés d'une même catégorie se rejoignent (« Arsenal W » et « Arsenal Women »), et une source qui ne marque qu'une des deux équipes est comprise. Test `unit_categorie`.
- 2026-09-06 - **Mode direct : la deuxième façon d'utiliser un lien de flux.** « Les embed.st, ça devrait être une deuxième façon d'utiliser un lien de stream, qu'on pourrait aussi avoir en mode normal, pour avoir les deux options. » Le mode normal (la page du site dans la tuile, nettoyée par le script) reste celui qui marche ; mais une page comme embed.st (640 Ko de script obfusqué, moteur pair-à-pair SwarmCloud) « charge, lag, roule, puis plante ».
  - Le script utilisateur, qui est dans le cadre, voit passer le MANIFESTE vidéo (.m3u8/.mpd) que le lecteur de la page demande — chronologie des ressources du cadre (`PerformanceObserver`), plus `extractM3u8Url` — et le remonte une fois à l'application (`media_url`), manifestes publicitaires exclus. Testé dans `test_cleaner`.
  - La tuile le retient pour ce lien (`direct_media` local, 3 h) et affiche « ▶ direct » ; un clic joue le manifeste dans notre propre `<video>` (hls.js), sans la page, sa régie ni son moteur pair-à-pair ; « 🖼 page » revient. Si rien ne joue en 25 s ou si hls.js rapporte une erreur fatale (CORS, 403, adresse expirée), la tuile revient d'elle-même au mode page, retient l'échec et le dit dans un toast. Deux échecs sur un lien, et le direct ne se propose plus d'office (`aProposer`). `js/directmedia.js`, testé dans `unit_directmedia`.
  - Ce qu'on ne sait pas encore, et qu'on observera plutôt que de deviner : quels CDN acceptent notre origine. Un CDN qui exige le référent du site répondra 403, et la tuile reviendra à la page ; le pont (`GM_xmlhttpRequest`, qui pose le référent) serait l'étape suivante pour ceux-là.
- 2026-09-06 - **Toutes les pages par sport de chaque source, pas seulement celles qu'on connaissait ; VIPLeague retrouvé sur vipleague.me.** « Sportsurge trouve plein de liens pour la MLB, ça devrait pour les autres sports aussi, genre tous. TOUS les domaines, sauf OnHockey, couvrent tous les sports : tu les checks tous. » Mesuré sur le passage de 15 h 27 : Sportsurge 13 pages, 23 matchs, tous de baseball dans le cache — sa liste n'avait AUCUNE page soccer, F1, tennis, golf ou cricket ; Streamed lisait 6 de ses 15 catégories (`api/sports`) ; VIPLeague « ÉCHEC … HTTP 404 » sur ses trois miroirs.
  - Sportsurge refuse les centres de données (403 ici comme pour WebFetch), on ne peut donc pas relever ses adresses de pages : `discoverPages` (js/config.js) fait lire à `getSourcePages` le menu de l'accueil déjà téléchargé et ajoute toute page « watch-<sport>-streams/ » qu'il annonce, le sport déduit du nom (`sportOfLeague`). Une page dont le sport est inconnu, ou marquée `other`, se lit quel que soit le programme du jour. Les deux appelants (navigateur et GitHub Actions) passent l'accueil.
  - Streamed : les 9 catégories manquantes ajoutées (motor-sports, tennis, rugby, golf, cricket, afl, darts, billiards, other).
  - VIPLeague : .vg/.io/.cc redirigent tous vers **vipleague.me**, la grille est sur `/watch-now` (`/live-now-streaming` répond 404) et les liens sous `/now-playing/<sport>/<slug>` au lieu de `/watch/…` ; `parseVipleague` lit les deux formes. `domains.json` et `SOURCE_MIRRORS` mis à jour.
  - Vérifié sur place : Buffstreams n'a pas d'autre page de liste que les 9 déjà lues (WWE et golf y sont des pages d'événement, le soccer renvoie vers un site tiers) ; Methstreams `nhlstreams`/`cfbstreams` redirigent toujours vers crackstreams.ms, injoignable, et `tna` est vide ; Footybite, Streameast, Flexfitness et MLBite+ listent tout sur l'accueil.
  - Test `unit_sourcepages`.
- 2026-09-06 - **Les popups se bloquent depuis le script utilisateur, dès le premier script de la page ; la tuile attend 90 s un hôte lent qui joue d'ordinaire.** « Ya moyen de bloquer les popups à même le Multivision ? » Depuis l'application, non : une iframe d'origine croisée sans bac à sable (retiré le 5 septembre à la demande de l'utilisateur) n'obéit à rien de déclaratif. Depuis le script, oui — à condition d'arriver le premier. Mesuré sur embed.st : la page pèse 640 Ko de script obfusqué, charge la régie Adcash (`aclib.runPop`) et un moteur pair-à-pair SwarmCloud ; la régie garde SA référence à `window.open` dès son premier script, et `injectPopupBlocker` tournait à `document-idle`, après elle.
  - `multiview-cleaner.user.js` 1.6 : `@run-at document-start`, blocage posé avant tout script de la page, `window.open` rendu non réassignable, écouteurs de clic sur `window` en capture (avant ceux du site). Le nettoyage et le signal de lecture attendent `DOMContentLoaded` comme avant. Défaut corrigé au passage : le bloqueur était rappelé à chaque passe de nettoyage (toutes les 500 ms pendant 15 s) et empilait ses écouteurs.
  - Test différentiel dans `test_cleaner` : sans le script, un vrai clic ouvre bien la fenêtre (sinon le test ne prouve rien) ; avec, ni `window.open`, ni un lien `_blank`, ni la référence gardée par le site n'ouvrent quoi que ce soit. Le banc sert le script comme premier script du document, comme Tampermonkey à `document-start`.
  - « embed.st, soit ça marche pas, soit c'est hyper long… finalement ça marche après x temps. » La tuile passait à la source suivante après 30 s sans vidéo — AVANT que embed.st ne démarre — puis la suivante repartait de zéro. `patienceMs` (js/playability.js) : 90 s pour un lien observé en lecture ou dont l'hôte joue le plus souvent, 30 s sinon. Test dans `unit_playability`.
- 2026-09-06 - **Le navigateur va chercher les liens lui-même quand le script utilisateur est là : à l'ouverture, puis quand on ouvre une carte.** Demande : « au lieu de GitHub Actions, l'application devrait aller fetch les liens à l'ouverture puis lorsqu'on ouvre une carte de match, ça a l'air de tout chier le GitHub Action ». Jusqu'ici, un cache serveur de moins de trois heures suffisait à SAUTER toute lecture en direct (`skipScraping`), même avec le pont ; et la relecture à l'ouverture d'une carte n'avait lieu qu'une fois par match et par session, pour les seuls matchs venus du cache.
  - `loadAllRun` (js/main.js) : avec le pont, le navigateur lit les sources à chaque passe espacée d'au moins 5 min, quel que soit l'âge du cache serveur — depuis SON adresse, celle qui passe là où le centre de données de GitHub est refusé (sportsurge répond 403 à tout serveur). Sans pont, règle inchangée (cache < 3 h ou passe < 15 min). Le cache reste affiché tout de suite : c'est le point de départ, pas l'attente.
  - Au démarrage, `waitForBridge(1500)` (js/embed-bridge.js) laisse au script le temps de s'annoncer avant la décision : le premier chargement se croyait toujours sans pont.
  - `doitRelireLaPage(m, now, pont)` (js/scrapers.js) : avec le pont, toute carte ouverte relit sa page de match si elle ne l'a pas été depuis 10 min (`pageLueA`, posé par `openMod`) ; sans pont, ancienne règle. Test `unit_relecture`, groupe 2 bis.
  - Ce que GitHub Actions garde : le cache de départ, la seule voie sans script, et la vérification des lecteurs.
- 2026-09-06 - **Footybite ne fournissait que le lien de repli : le dépôt le lisait encore sur `.bid`.** « C'est .im le domaine footybite. » `domains.json` — lu au démarrage par l'application ET par le scraper, et prioritaire sur `js/config.js` — disait encore `footybite.bid`. Or `.bid` ne fait que rediriger vers `.im` (la page d'accueil livrait donc ses matchs, aucun miroir n'était promu), ses pages de match répondent 403, et la liste des pages bloquées écarte justement `.bid` : le scraper ne lisait AUCUNE page de match footybite. Mesuré sur le cache de 13 h 07 : 33 liens « footybite » sur 76 matchs, tous des « Page du match ».
  - `domains.json` et `SOURCE_MIRRORS` : `https://footybite.im/` seul ; `.bid` retiré partout (demande de l'utilisateur), il reste seulement dans la liste des pages bloquées.
  - Vérifié par un passage complet depuis ici : footybite 304 pages de match lues sur 304, **346 liens réels** sur 74 matchs (Everton–Manchester United : 53, Parma–Monza : 45, Frosinone–Venezia : 48), contre 0 avec `.bid`.
  - Le scraper adopte l'**origine canonique** qu'une page déclare (`<link rel="canonical">`, sinon `og:url`) avant d'analyser la page d'accueil, et la retient comme gagnante pour `domains.json` : un site qui déménage le dit lui-même, et l'ancienne adresse qui « répond encore » ne masque plus le déménagement. `canonicalOrigin` (js/config.js), un groupe de tests dans `tests/unit_mirrors.test.js` (8).
- 2026-09-06 - **Le cache des flux avait deux heures de retard : le passage de 14 h 10 n'a pas pu pousser.** Signalé comme « plusieurs matchs de soccer européen n'ont qu'un petit pourcentage des flux disponibles ». Mesuré : la fusion ne perd rien (0 lien dans des entrées non appariées pour les 22 matchs européens de la grille) ; c'est le cache lui-même qui était mince, et vieux. Le passage horaire de 14 h 10 avait bien produit un cache frais, mais `main` avait bougé pendant ses huit minutes (fusions de PR, commit de la grille) et l'action de commit ne rebase pas : « non-fast-forward », cache perdu, rien de nouveau jusqu'au passage suivant.
  - Les deux workflows se replacent désormais sur `main` avant de commiter (stash → pull --rebase → pop ; en cas de conflit sur le fichier produit, la version fraîche l'emporte).
  - Cadence des flux portée à **deux passages par heure** (:17 et :47) : les sites publient leurs liens dans l'heure qui précède le coup d'envoi — la page footybite d'Arsenal–Chelsea, vingt minutes avant le match, ne portait encore qu'un lecteur — et un cache horaire les manquait pour la moitié des matchs. Un passage dure ~13 min.
- 2026-09-06 - **La course de F1 disparaissait de Live à mi-course.** Signalé pendant le Grand Prix d'Italie. Pour une épreuve, `scripts/scrape_schedule.mjs` et `js/api.js` lisaient l'état de l'**événement** ESPN — le week-end entier — au lieu de celui de chaque séance : ESPN disait « post / Final » pour l'événement alors que la course était « in / In Progress ». Toutes les séances sortaient « terminées », course comprise. De plus, toutes étaient datées du jour demandé : les essais du vendredi apparaissaient « aujourd'hui ». Corrigé : l'état et la date viennent de la séance (`comp.status`, `comp.date`). Vérifié en régénérant la grille : Race 09:00 `live`, FP1/FP2 datées du 4, FP3/Qual du 5.
- 2026-09-06 - **La tuile charge la page entière ; le script nettoie ; Firefox se règle à part.** Décision de l'utilisateur, après des semaines sans vidéo : « les embed, ça chie, remet les pages complètes que le script nuke, plus de trucs direct vers vidéos, reconstruire marche pas du tout ». Puis : « les embed marchent dans Chromium, c'est Firefox qu'on doit réparer ; les reconstructions de page marchent jamais ».
  - **Tuile** (`fallbackToIframe`, js/multiview.js) : l'iframe charge l'adresse du lien telle quelle et demande le nettoyage au script utilisateur. Retirés : le lecteur extrait (`playerUrl`), la page reconstruite en `srcdoc` (tour de passe-passe), les bandeaux et la barre d'échec, le réglage « Reconstruire les pages non intégrables ». Une seule affectation de `src` subsiste, verrouillée par `tests/unit_blocked.test.js` (réécrit).
  - **Sources** : un lien « Page du match » ou marqué `topLevel` est une source à part entière — c'est exactement ce qu'on charge. `compterFluxUtiles` compte tout lien avec une adresse ; `tileTarget` rend toujours la page. Les cartes qui affichaient la loupe « aucun lien » alors qu'elles avaient une page en retrouvent une.
  - **Serveur** : plus d'extraction de lecteurs en chaîne dans `scripts/scrape_streams.mjs` (huit minutes par passage pour des lecteurs qui ne jouaient pas). Le sondage des en-têtes de cadre (`readFramePolicy`, `hostPolicy`) reste, informatif. Quatre tests qui verrouillaient l'extraction sont supprimés (`unit_chaine`, `unit_extractbudget`, `unit_lecturebornee`, `unit_serverplayer`). Le vérificateur observe désormais les pages, puisque ce sont elles que la tuile charge.
  - **Reste à retirer** : les fonctions de reconstruction de js/embed-bridge.js (`buildEmbedDocument`, `pickEmbeddablePlayer`, `resolveBlockedEmbed`, `installerReconstructionRecursive`) n'ont plus d'appelant dans l'application ; elles restent en place avec leurs tests (`unit_links`) pour ne pas mêler leur suppression à ce changement de comportement. Le test Playwright `test_recursif.spec.js`, qui éprouvait la reconstruction DANS l'application, est supprimé.
  - **Firefox** : section ajoutée à la page d'installation (index.html) et à la modale (js/multiview.js), avec les trois blocages et leur réglage — pages qui refusent l'iframe → extension « Ignore X-Frame-Options Header » ; lecture automatique et en arrière-plan → `media.autoplay.default = 0`, `media.autoplay.blocking_policy = 0`, `media.block-autoplay-until-in-foreground = false` ; protection contre le pistage → la désactiver pour le site de l'application. Étape suivante, à éprouver avec l'utilisateur sur Firefox.
- 2026-09-06 - **Premier passage réel du vérificateur (GitHub Actions, 13 h 08 UTC) : 87 cibles observées en 305 s — 54 jouent, 25 rien, 8 bloquées.** Par hôte : `embed.st` 4/4, `emb.apl503.me` 4/4, `vertex.st` 4/4, `cdnlivetv.is`/`.tv` 4/4, `rutube.ru` 4/4 ; `embed.sportspatrika.com` 0/4, `embedsports.me` 0/4, `flexfitness.fit` 0/4, `dcbbwymp1bhlf.cloudfront.net` 0/4, `youtube.com` 0/4 (depuis un centre de données, YouTube demande de prouver qu'on n'est pas un robot).
  - **Défaut trouvé en lisant le journal, corrigé** : l'expression de trafic vidéo s'appliquait à l'adresse ENTIÈRE. Elle comptait donc `cdn.jsdelivr.net/npm/@swarmcloud/hls/p2p-engine.min.js` et `static.rutube.ru/…/hls/1.6.15/hls.min.js` (« /hls/ » dans le chemin d'une bibliothèque), et la page de sonde elle-même quand l'adresse encodée de la cible finissait en « .m3u8 ». Une douzaine des 54 « joue » venaient de là. `isMediaRequest` (js/playability.js) juge désormais sur le chemin seul, écarte les scripts, styles et images, et le vérificateur ignore sa propre page. Un groupe de tests de plus dans `tests/unit_playability.test.js` (8 au total), sur les adresses exactes du journal.
  - Ce que ça change dans l'application : les liens `embed.st` (premier hôte du cache) passent devant ; ceux des hôtes 0/4 passent derrière l'inconnu, sans être exclus — le registre local du navigateur (script v1.5) peut les remonter s'ils jouent chez l'utilisateur.
- 2026-09-06 - **Nouveau mécanisme d'amenée des vidéos dans le Multivision : la jouabilité observée.** « Ça joue pas depuis plusieurs semaines, faut un nouveau système. » Mesuré avant de redessiner : sur les 1 237 cibles de tuile du cache, 39 hôtes couvrent tout, et presque tous servent une page intermédiaire (une iframe vers le maillon suivant), une page anti-adblock, ou répondent 403 — rien de cela ne se lit dans l'adresse, et `sortFluxLinks` classait par qualité annoncée et nom de site. Le Multivision ouvrait donc d'abord ce qui « faisait bien » et ne jouait pas.
  - **Principe** : on ne devine plus, on observe. Trois pièces, une règle commune dans `js/playability.js` (sans import).
  - **Serveur** (`scripts/verify_players.mjs`, nouvelle étape du workflow horaire) : un vrai Chromium charge les lecteurs des matchs en direct ou imminents dans une iframe et note trafic vidéo, `<video>` prêt, cadre refusé → `verified` par lien, `hostPlay` par hôte, dans data/streams.json. Budget 5 min, 150 cibles, hôtes distincts, jamais bloquant.
  - **Classement** : choix de l'utilisateur d'abord, puis observé > hôte fiable > inconnu > rien > bloqué, puis seulement la forme.
  - **Tuile** : « source k/n », bouton ⏭, et — script utilisateur v1.5 installé — signal « vidéo en lecture » depuis le cadre du lecteur (le seul endroit d'où l'on voit), qui marque la source, alimente le registre local, et déclenche seul le passage à la source suivante après 30 s sans image.
  - **Ce qui n'a pas pu être vérifié ici** : le navigateur du bac à sable n'atteint aucun site externe. Le vérificateur s'éprouve dans GitHub Actions ; son journal dit combien de cibles ont joué.
  - Tests : `tests/unit_playability.test.js` (7 groupes), un test de plus dans `tests/test_cleaner.spec.js` (signal de lecture) ; 33 fichiers unitaires et 19 tests Playwright passent. `sw.js` : `sports-guide-v5`, `js/playability.js` dans la coquille.
- 2026-09-06 - **L'onglet Live n'avait pas de sens : la grille est faite par ESPN et les sources acceptées, pas par les scrapers.** Constaté à 22 h 15 sur les vraies données : « Money In The Bank » (WWE) et un T20 Émirats–Singapour en DIRECT, « NFL Schedule Release 2021 », « NFL Network vs NFL RedZone », « Toronto Raptors vs Denver Nuggets 18:30 » un samedi de septembre. Tous des flux **sans match dans la grille** : footybite marque « Match Started » des galas vieux de plusieurs mois sans donner d'heure (le parseur posait l'heure du moment, rajeunie à chaque passage horaire), et Buffstreams remplit ses pages NFL et NBA hors saison de lignes sans date (« Washington Redskins »).
  - Règle, sur décision de l'utilisateur : un flux sans match dans la grille **ne crée plus de carte**, quelle que soit sa ligue. `mergeFluxToApi` (js/api.js) le garde dans `S.unmatchedStreams` et dans le journal des sources, pour le diagnostic. Le bloc « ligue déjà couverte par l'API → Autres Flux, sinon vraie ligue » disparaît avec lui. Une première version, plus timide (expiration des « en direct » de plus de quatre heures, rejet des ligues qu'ESPN couvre intégralement), a été écrite puis retirée : la règle simple la contient.
  - Conséquence à connaître : un sport qu'aucune source de calendrier ne couvre (KHL, Liiga, cricket…) n'apparaît plus, même quand un scraper en a les liens. C'est le prix de la règle ; en ajouter la couverture se fait côté calendrier.
  - `tests/unit_merge.test.js` réécrit (4 groupes) : aucune carte, ligue présente ou absente, grille vide ; les flux écartés exposés et remis à zéro. Les 18 tests Playwright passent, dont ceux de l'onglet Live.
- 2026-09-06 - **Multivision : un lecteur qui répond 403 n'est pas un lecteur.** En suivant la chaîne des lecteurs, `scripts/scrape_streams.mjs` ne lisait que l'intégrabilité (en-têtes de cadre) de l'hôte candidat : `chatgpt.hereisman.net`, qui répond 403 à tout le monde (avec ou sans référent, vérifié), restait le `playerUrl` de **54 liens** de MLBite, et la tuile affichait la page d'erreur du navigateur. Un hôte qui répond 4xx/5xx est écarté, le passage suivant essaie le maillon suivant ; les hôtes écartés sont comptés dans le journal du passage.
  - **Vérifié, et non vérifié.** Parcours réel joué dans un navigateur sur les données du jour : la carte « Seattle Sounders vs Red Bull New York » s'ouvre, « Ajouter au Multivision » pose une tuile, et la tuile charge bien le lecteur extrait par le serveur (`embed.sportspatrika.com/…embed.php?ch=es149`, badge « lecteur ») — c'est la correction de la veille qui opère. Ce que je **n'ai pas pu** voir : la vidéo jouer. Le navigateur du bac à sable n'atteint aucun site externe (connexion coupée par le proxy, même pour example.com), là où `curl` passe. En téléchargement direct, `embed.st` (265 liens, premier hôte) sert un vrai lecteur Clappr/JW ; `embed.sportspatrika.com` (168 liens) annonce `frame-ancestors *`, donc s'encadre, et emboîte `channel.php`. À éprouver dans un vrai navigateur, sans le script utilisateur d'abord.
- 2026-09-06 - **« On tourne en rond » : trois défauts distincts derrière « les liens ne vont pas dans les bons matchs et le Multivision n'affiche rien ».** Chacun mesuré sur le cache du jour (660 matchs) et sur la page réelle de footybite, puis reproduit par un test.
  - **Footybite : 104 matchs sur 161 étiquetés « MLB »** — la MLS, La Liga, l'USL, la Liga peruana et **tout le football universitaire**. Le payload Next.js est une suite de lignes « `<id>:` » qui se référencent par `"$L<id>"` ; une section n'écrit en clair que ses premières lignes, les autres sont des références résolues APRÈS toutes les sections. « La dernière section rencontrée avant la ligne » leur donnait donc à toutes la dernière du fichier, « mlb ». Sur la page réelle : **120 matchs sur 178** ; après correction, 28, tous de vrais matchs de MLB. `parseFootybite` remonte désormais la parenté (`sectionDe`) : la ligne qui référence celle-ci, puis la sienne, jusqu'à une section.
  - **Conséquence en chaîne, corrigée aussi : les villes universitaires devenaient des clubs professionnels.** Avec un sport faux, `getOfficialTeamName` résolvait « Texas » en **Texas Rangers**, « Florida » en **Florida Panthers**, « Memphis » en **Memphis Grizzlies**, « San Antonio » (USL) en **Spurs** : le cache portait « Texas Rangers vs Texas State », « Florida Panthers vs Florida Atlantic ». Un tel nom ne retrouve jamais son match ESPN, et la carte est fausse. `officialTeamNameForLeague` (js/scrapers.js) : en compétition universitaire, aucune résolution (la base ne recense que les clubs professionnels) ; dans un sport connu, la résolution est refusée si elle change de sport (3ᵉ argument de `getOfficialTeamName`, déjà câblé pour OnHockey).
  - **Un gala de la WWE portait les liens de la F1 et du MotoGP.** Le raccourci Racing/Event d'`isMatchPair` retire les termes génériques puis compare par inclusion : « F1 Main Race » ne laisse que « main », que « Sunday Nights Main Event » contient. Le MotoGP suivait par ricochet, fusionné dans la même entrée. L'inclusion exige désormais un résidu d'au moins cinq caractères des deux côtés ; en dessous, seule une vraie similarité (`isMatch` strict) apparie. « Italian Grand Prix » continue d'apparier ses séances.
  - **Multivision : le lecteur extrait par le serveur n'atteignait jamais la tuile.** Une entrée du Multivision ne porte que `{ url, name, mid }` (c'est tout ce que reçoit `addToMultivision`), mais la décision de chargement lisait `s.playerUrl` et `s.topLevel` sur cette entrée — jamais copiés. Le champ `playerUrl` que le scraper horaire écrit dans data/streams.json, et la mesure « refuse l'iframe », étaient lettre morte : la page de match était chargée telle quelle et le navigateur affichait son refus. Sur le cache du jour : **511 liens** portaient les deux champs, **100 matchs** n'avaient QUE ce type de lien — pour eux, rien ne s'affichait. `lienDuMatchPourFlux` (js/multiview.js) retrouve le lien dans le match (par `mid`, sinon par adresse dans toute la grille) au moment de charger la tuile — plutôt que de recopier les champs à l'ajout, pour qu'une entrée restaurée du stockage local ou changée par le sélecteur reste juste.
  - **Même défaut de nom dans les autres parseurs.** Buffstreams résolvait « Memphis » (page CFB) en Grizzlies, si bien que « Memphis vs Arkansas State » fusionnait avec « Memphis Grizzlies vs Golden State Warriors » (NBA). `officialTeamNameForLeague` est appliqué aux sept parseurs qui connaissent la compétition du match (footybite, buffstreams, sportsurge, vipleague, methstreams, flexfitness, streamed, SportsDB).
  - **Deux sports différents ne s'apparient plus, quels que soient les noms.** L'exclusion d'`isMatchPair` ne connaissait que hockey, basket, baseball et football américain : « Miami FC vs Pittsburgh Riverhounds » (USL) recevait les liens de « Miami vs Pitt » (football universitaire), « pitt » étant contenu dans « pittsburghriverhounds ». `sportOfLeague` **déménage de js/config.js vers js/db.js** (module sans dépendance d'interface, ré-exporté par config.js pour ses appelants) pour que js/match.js s'en serve sans créer de cycle ; `sportFamily` regroupe les étiquettes d'une même compétition (« F1 » / « Motorsport », « American Football » / NCAAF, NBA / NCAAB, MMA / boxe). « other » n'exclut rien.
  - **Validation croisée alignée, équipe par équipe.** L'ancienne comptait les mots du match court retrouvés N'IMPORTE OÙ dans l'autre : « Kent State vs South Carolina » obtenait 3 mots sur 4 face à « Florida A&M vs South Carolina State », « McNeese State vs Texas Wesleyan » 2 sur 2 face à « Texas State vs Texas ». Désormais chaque équipe du match court doit voir TOUS ses mots désignants (ni génériques — state, north, south, saint… — ni trop courts) dans l'équipe alignée du match long, dans un sens ou dans l'autre. Mesuré sur les pages réelles footybite × buffstreams (178 × 73 matchs) : 44 paires dont 6 fausses avant, 38 paires toutes justes après.
  - **La grille ESPN n'avait que des surnoms pour les universités : aucun flux de football universitaire n'y trouvait jamais sa place.** `scripts/scrape_schedule.mjs` et `js/api.js` lisaient `team.name` (« Longhorns », « Dukes », « Flames ») ; pour un club professionnel la base retrouve « Detroit Tigers » depuis « Tigers », pour une université elle ne retrouve rien. Le cache du 5 septembre portait « Dukes vs Flames », et **0 des 68 matchs de football universitaire** n'appariait un flux — alors que footybite, buffstreams et sportsurge en livrent plus de 150. Les deux lisent maintenant `team.displayName` (« Texas Longhorns »). Mesuré sur la page réelle de footybite face à la grille régénérée : **42 → 81 matchs appariés, dont 0 → 40 en football universitaire**, aucun appariement douteux.
  - Tests : `tests/unit_footybitesections.test.js` (nouveau, 6 groupes — structure réelle du payload, lignes différées, noms universitaires), `tests/unit_mvlien.test.js` (nouveau, 5 groupes), trois groupes de plus dans `tests/unit_matchmerge.test.js` (8 au total) ; `tests/unit_blocked.test.js` suit la nouvelle règle de décision. Les 32 fichiers de tests unitaires et les 18 tests Playwright passent.
  - Reste connu : footybite liste le même match pour aujourd'hui et demain sous deux adresses, que `mergeMatches` replie en une seule entrée sans date ; les sources qui répondent 403 depuis un serveur (sportsurge, vipleague ce jour) ne livrent rien au cache, seul le navigateur les lit ; et « Texas vs Rice » s'apparierait encore à « Texas State vs Rice » (inclusion d'un nom dans l'autre), le choix entre plusieurs candidats se faisant alors par l'heure.
- 2026-09-04 - **Compétitions homonymes entre sports, et capitalisation des accents.** Suite de la demande « toutes les ligues complètes et claires », dont l'exemple était la Champions League de hockey confondue avec celle de football.
  - **Homonymes.** « Champions League », « Club Friendly », « Super Cup », « Malmö Arena Cup » existent dans plusieurs sports, et rien dans le nom ne dit lequel : `sportOfLeague`, qui ne voit que le nom, les range tous en football à cause des mots « league » et « cup ». Sur le cache du 4 septembre, les six matchs de la Champions **Hockey** League étaient ainsi classés — et affichés — avec le football.
  - La source, elle, connaît la réponse : OnHockey ne diffuse que du hockey. `qualifyLeagueForSport` (js/scrapers.js) annote donc le nom quand il ne se classe pas déjà dans le sport de la source, avec le vrai nom quand on le connaît (« Champions Hockey League ») et une annotation sinon (« Club Friendly (Hockey) »). La règle se corrige d'elle-même : une ligue déjà sans ambiguïté (`Liiga`, `PWHL`, `KHL`) n'est pas touchée. `sportOfLeague` lit ensuite le sport dans le nom annoté — **le classement et l'affichage sont corrigés du même coup, sans nouvel argument à faire circuler**.
  - **Accents.** `formatLeagueName` capitalisait avec `/\b\w/g`. Sans le drapeau `u`, `\w` ne considère pas les lettres accentuées comme des lettres : une frontière de mot tombait donc au milieu de « pohár », juste avant le « r », et le nom s'affichait **« PoháR PrimáTora »**. La capitalisation suit désormais les séparateurs réels, avec `\p{L}` : « Pohár Primátora », « Malmö Arena Cup », « São Paulo ».
  - Quatre groupes de tests de plus dans `tests/unit_sports.test.js` (10 au total).
  - Reste connu : `Cfb` et `Ncaaf` sont du même sport mais restent deux libellés, donc deux sections ; et la résolution des noms d'équipes ignore le sport du contexte (« Lahti Pelicans » résolu en « New Orleans Pelicans », NBA).
- 2026-09-04 - **Appariement des équipes : le sport du contexte, et les doublons masquants.** Demande : que ligue, équipe et ville travaillent ensemble pour l'appariement, et que tout ait couleurs et logos.
  - **Sport ignoré.** OnHockey annonce « Pelicans » pour un match de Liiga, et l'alias « pelicans » — que seuls les New Orleans Pelicans revendiquent dans la base — le résolvait en équipe NBA : la carte affichait « New Orleans Pelicans vs Vaasan Sport » dans un championnat finlandais. Ce n'est **pas** une ambiguïté entre deux entrées (vérifié : une seule équipe porte cet alias) — le club de Lahti n'est pas dans la base, et l'alias d'un autre sport a comblé le vide. `getOfficialTeamName` accepte désormais le sport du match et rend le nom d'origine plutôt qu'une équipe d'un autre sport : non résolu, mais jamais faux — même parti pris que pour les alias ambigus. Câblé dans `parseOnHockey`, seul parseur dont le sport est certain.
  - **Doublons masquants.** `TEAM_DATA` contient des entrées nées de noms mal analysés puis enregistrés comme de nouvelles équipes : « parissaintgermain » à côté de « paris saint-germain », « rennes » / « stade rennais », « auxerre » / « aj auxerre », « blue jays » / « toronto blue jays ». Le doublon ne porte qu'une vignette fabriquée à partir du nom. L'index `NORM_TEAM_KEYS` étant construit par simple affectation, il gardait la **dernière** clé rencontrée — le doublon — et **masquait le vrai blason du club**. L'index préfère maintenant l'entrée qui porte un vrai blason ; à égalité, l'ordre d'insertion de teams.js est conservé. Quatre clubs concernés, tous très visibles.
  - **Couverture des blasons, mesurée** : sur les 819 équipes du cache, **19 % ont un vrai blason**, les autres une vignette générée. La correction ci-dessus n'en récupère que quelques-unes : le gros du manque n'est pas un défaut d'appariement mais un **manque de données** — `TEAM_DATA` stocke elle-même des vignettes générées pour beaucoup d'équipes (« Ipswich Town », par exemple, alors que c'est un club de Premier League). Aucune équipe n'est jamais sans image ni sans couleurs : il y a toujours un repli. Combler les 81 % demande d'enrichir la base, pas de corriger du code.
  - `tests/unit_pairing.test.js` (nouveau, 7 groupes). Les deux corrections ont été éprouvées à l'envers : en retirant la préférence de l'index, puis le contrôle de sport, chaque test échoue avec le bon message.

- 2026-09-04 - **La moitié du classement par sport était morte : 36 caractères de contrôle à la place des ancres `\b`.** Demande de départ : « je veux que tu fasses toutes les ligues complètes et claires », avec pour exemple la Champions League de hockey confondue avec celle de football.
  - Cause trouvée en instrumentant, pas en relisant. Dans `sportOfLeague` (js/config.js), les ancres `\b` avaient été remplacées **dans le fichier source** par le caractère de contrôle U+0008 (« retour arrière ») — le résultat d'un outil ayant interprété l'échappement au lieu de l'écrire. Les expressions cherchaient donc ce caractère, qu'aucun nom de ligue ne contient : **toutes les alternatives ainsi encadrées étaient mortes**.
  - Ce qui rendait le défaut retors, et m'a fait tourner en rond un moment : seuls les **sigles** étaient encadrés, si bien que la moitié de la fonction marchait (baseball, hockey, soccer passaient) ; et afficher la source avec `console.log` ne montrait rien, le terminal interprétant le retour arrière comme un effacement — j'ai lu plusieurs fois un code qui semblait juste. Il a fallu tracer chaque branche pour voir que la 21ᵉ (`return 'other'`) était seule atteinte, puis lire les octets.
  - **Effet mesuré sur le cache du 4 septembre : 224 matchs sur 503 classés « other »**, dont les 124 de football universitaire, 29 de MLB et 17 de NFL — donc rangés dans « Autres streams » au lieu de leur sport. Après correction : **79**. Les 36 occurrences étaient toutes dans cette seule fonction ; aucun autre fichier du dépôt n'est touché.
  - Motifs manquants ajoutés dans la foulée, mesurés sur les mêmes données : `ncaaf` (62 matchs), `friendly` (5), `primera` (2).
  - `tests/unit_sports.test.js` (nouveau, 6 groupes) couvre **chaque sigle**, précisément parce que ce sont eux qui étaient morts, et **refuse tout caractère de contrôle dans la fonction** — c'est le test qui aurait attrapé la panne, et le seul qui la rattraperait si un outil réintroduisait la substitution. Vérifié en réintroduisant une seule ancre cassée : il échoue.
  - Restent connus et non traités : « Champions League » reste classé football alors que les 6 matchs du cache sont du hockey (il faut un indice de sport venu de la source), et `Cfb`/`Ncaaf` restent deux libellés pour une même ligue à l'affichage.

- 2026-09-04 - **Un moteur d'extraction en double, supprimé.** `resolveStreamUrl` (js/utils.js) contenait un second extracteur écrit à la main : première `<iframe>` dont le `src` ne contient pas « ads », puis expressions régulières sur les charges Next.js (`directStreams`, `iframeStreams`). Il était déclenché par `isAggregateSite`, **une liste figée de douze domaines** — or ces sources changent d'adresse plusieurs fois par saison, et la liste cessait alors silencieusement de correspondre. Quand elle correspondait, ce moteur passait **avant** celui de `js/extractors.js` et le court-circuitait.
  - `js/extractors.js` fait déjà tout cela, en mieux : six stratégies de récolte, un pointage, aucun nom de site en dur, et des tests. Son propre en-tête explique pourquoi une branche par site ne tient pas dans le temps.
  - La suppression est sûre parce que **les deux appelants enchaînent déjà sur le moteur générique** : `resolveBlockedEmbed` pour le Multivision, `scrapeMatchFlux` pour l'ajout manuel d'un flux. `resolveStreamUrl` passe de 174 à 123 lignes et ne garde que ce qu'aucun extracteur ne peut deviner — YouTube, Twitch, et les règles définies par l'utilisateur dans l'Investigateur.
  - **Régression évitée au passage** : le réglage « Reconstruire les pages non intégrables » gouvernait aussi l'EXTRACTION du lecteur. Or extraire une adresse et la charger normalement n'est pas une reconstruction — c'est la voie ordinaire, et la seule depuis cette suppression. Le décocher aurait désormais privé de tout lecteur. Le réglage ne gouverne plus que la branche `srcdoc`.
  - Trois groupes de tests de plus dans `tests/unit_fetchbridge.test.js` : une page d'agrégateur est rendue telle quelle, les conversions YouTube et Twitch survivent, et les entrées vides ne lèvent rien.

- 2026-09-04 - **Plus aucune extraction ne fonctionnait : les quatre transports de `fetchPage` étaient morts en même temps.** Signalé comme « aucune des extractions du multiview ne fonctionne ». Mesuré, pas supposé :
  - `allorigins` rend **500/522** — y compris sur `example.com`, donc c'est le service qui est mort, pas les cibles ; `codetabs` rend **522** de même ; `proxy.cors.sh` est passé derrière un **challenge Cloudflare** qui rend 403 dès qu'un en-tête `Origin` est présent, c'est-à-dire pour **toute requête venue d'un navigateur** (sans cet en-tête, en curl, il répond encore 200 — d'où un diagnostic trompeur au premier essai) ; le transport `direct` est bloqué par CORS dans un navigateur, par conception.
  - Les trois voies d'extraction — recherche des flux d'un match, résolution d'un lien d'agrégateur (`resolveStreamUrl`), tour de passe-passe — partagent toutes `fetchPage`. Aucun transport disponible signifie donc : rien ne marche, nulle part.
  - **Ce n'était pas le bac à sable des lecteurs** (#502), mon premier soupçon : je n'ai pas pu le mettre hors de cause par l'expérience — cet environnement ne charge aucun lecteur tiers dans un navigateur, avec ou sans proxy — mais la panne des transports explique le symptôme en entier.
  - Correctif avec ce qui existait déjà : le **pont du script utilisateur** (`GM_xmlhttpRequest`, écrit et éprouvé en #501) n'est pas soumis à la politique d'origine croisée et ne dépend d'aucun service tiers. Il n'était câblé que dans le tour de passe-passe ; il devient le **premier transport de `fetchPage`**, avec repli sur les proxys. Toutes les voies d'extraction sont réparées d'un coup, pour qui a le script en version 1.2.
  - Le bandeau d'échec du Multivision **nomme désormais la cause** : sans pont ET sans proxy, ce n'est pas cette page qui résiste, c'est que l'application n'a plus aucun moyen de télécharger quoi que ce soit — et rien d'autre ne marchera non plus. L'interface n'en disait rien jusqu'ici.
  - `tests/unit_fetchbridge.test.js` (nouveau, 3 groupes) rejoue le protocole `postMessage` du script plutôt que de remplacer la fonction, pour éprouver le câblage réel. Éprouvé à l'envers : sans le câblage, le test échoue.
  - **Limite à connaître** : pour un utilisateur sans le script, il n'y a aujourd'hui aucun transport public en état. Le réglage « proxy personnalisé » des Options reste la seule issue.

- 2026-09-04 - **Les domaines morts se détectent et se remplacent tout seuls.** `fetchWithMirrors` basculait déjà sur un miroir vivant, mais seulement pour l'exécution en cours : rien n'était écrit, si bien que le lancement suivant réessayait le domaine mort en premier et repayait son délai d'attente — et le navigateur, qui lit `domains.json` au démarrage, continuait de partir sur la mauvaise adresse jusqu'à correction manuelle.
  - **Défaut trouvé en testant ma propre implémentation** : le script serveur **ne lisait jamais `domains.json`**. Il partait de la liste figée de `js/config.js`, donc il n'éprouvait pas l'adresse réellement en service et ma « promotion » ne comparait que des valeurs. `applyDomainsFile()` applique désormais le fichier au démarrage, exactement comme `fetchRemoteConfig` côté navigateur.
  - **Second défaut, plus profond, trouvé en éprouvant le garde-fou** : un domaine expiré puis racheté répond 200. Le script s'y arrêtait et n'essayait jamais le miroir vivant — la source produisait 0 match en silence, précisément la panne qu'on cherche à détecter. Vérifié en plaçant `example.com` en tête des miroirs de Methstreams. On juge donc une adresse sur les **matchs livrés**, pas sur son code HTTP : tant qu'aucun match ne sort, on passe au candidat suivant. Une source saine livre dès le premier et ne paie rien de plus.
  - Écriture encadrée par deux règles : `reorderCandidates` rend une **permutation** de l'entrée (aucune adresse inventée, le script ne peut pas être détourné vers un domaine arbitraire), et `shouldPromoteSource` exige `matches > 0`. Un domaine mort est **relégué en queue, pas supprimé** — ces adresses reviennent, mais tant qu'elles sont mortes, les garder devant coûte un délai d'attente par heure.
  - Vérifié en conditions réelles : avec un domaine injoignable en tête, la source bascule et `domains.json` est réécrit (`vipleague-domaine-mort.invalid` → `vipleague.vg`, 12 matchs) ; avec `example.com` en tête, la source bascule aussi (`methstreams.gs`, 23 matchs) et le domaine piégé part en queue. Bénéfice inattendu : `STREAMED_URL`, absent du fichier depuis son ajout, s'y est écrit tout seul.
  - Le workflow horaire commite `domains.json` en plus de `data/streams.json`, sans quoi l'apprentissage serait perdu à chaque exécution.
  - **Fragilité de test corrigée au passage** : le figeage d'horloge se calait sur le `generatedAt` du cache, ce qui ne garantit pas qu'il y ait des matchs en direct à cet instant. Constaté sur un cache généré à 23 h EST, où tout le programme était encore à venir : le test des rails d'affiches tombait. L'instant est désormais choisi **d'après les données** — celui où le plus de matchs sont en cours ou imminents, l'heure locale passant par le même fuseau que l'application. Éprouvé sur un cache simulé à 5 h du matin : 11 tests sur 11 passent.
  - `tests/unit_mirrors.test.js` (nouveau, 7 groupes, sans réseau ni système de fichiers).

- 2026-09-04 - **Le menu des sites était pris pour des flux.** Question posée : où trouver d'autres sources fiables ? La mesure a répondu autre chose — le problème n'était pas le nombre de sites mais le rendement de ceux déjà en place. Sur le cache du 4 septembre : Buffstreams 109/109 matchs jouables, VIPLeague 12/12, Footybite 24/35, mais **Sportsurge 3/40 (7 %)**, **OnHockey 2/27**, **MLBite+ 0/11** — pour 640 liens « page » inexploitables.
  - Cause trouvée dans `NAV_PATH_RE` (`js/extractors.js`), qui écarte les chemins de navigation : sa classe de caractères n'acceptait **pas les chiffres**. `/watch-nfl-streams/` était bien écarté, mais `/watch-ligue-1-streams/`, `/watch-f1-streams/` et `/watch-peruvian-liga-1-streams/` passaient au travers. Le **menu** de Sportsurge et Soccersurge se retrouvait donc attaché aux matchs comme s'il s'agissait de flux : une rencontre de football universitaire (« Rockford Regents ») portait des liens vers la Ligue 1 et la F1.
  - Correctif : `[a-z-]*` → `[a-z0-9-]*`. Évalué sur l'ensemble du cache **avant** de l'appliquer, avec la vraie condition du code (l'exception `-vs-|/game/|/embed|/watch/` comprise) : **180 liens de menu écartés, 0 vrai lecteur touché**. Les pages de match qui finissent elles aussi par « -stream » (`mlbbite.plus/watch/live/…-free-live-stream`) sont protégées par cette exception, un test le vérifie.
  - **Second saut côté serveur : mesuré, puis abandonné.** L'idée était d'appliquer au scraper ce que le Multivision fait déjà — télécharger une page « page » et en extraire le lecteur. Le gisement restant après le correctif se réduit à quatre hôtes, et aucun ne paie : `onhockey.tv` (207 liens) est **déjà téléchargé** par le scraper, `mlbbite.plus` (121) ne contient **aucun lecteur** dans son HTML — vérifié en téléchargeant une page, elle ne porte que des liens vers ses propres pages sœurs —, `hockeyslovakia.sk` (69) et `footybite.bid` (43) répondent **403** à un serveur. C'est structurel : ces hôtes ne servent le lecteur qu'à un vrai navigateur, ce qui est précisément la raison d'être du pont côté client.
  - Deux groupes de tests de plus dans `tests/unit_extractors.test.js` (15 au total).

- 2026-09-04 - **Nouvelle source `streamed.pk` : catch, boxe et football universitaire.** Manque mesuré sur le relevé du 3 septembre : **aucun match de catch** (les pages AEW/WWE de Methstreams sont des calendriers vides — « les liens sont ajoutés 1 h avant l'événement » — et `league/cfbstreams` redirige vers une page « Go Back to Homepage »), **6 matchs de boxe**, **43 de football universitaire**.
  - `streamed.pk` est la seule source à exposer une **API JSON** plutôt que des pages HTML à deviner : `/api/matches/<catégorie>` liste les événements, `/api/stream/<source>/<id>` donne les flux. Ses lecteurs (`embed.st`) ne posent **ni `X-Frame-Options` ni CSP** : ils s'intègrent directement.
  - Exécution réelle du scraper : **376 matchs** depuis cette seule source, `embed.st` devenant le premier hôte de lecteurs (251 liens). Football universitaire **43 → 81 matchs, 52 jouables** ; boxe **6 → 14** ; catch **0 → 2** plus AEW et WWE dès que leur date arrive (vérifié : « AEW Saturday Night Collision » est bien produit, classé AEW).
  - Deux corrections au moteur d'extraction, générales et pas propres à cette source : il ne lisait le JSON que dans les `<script>` d'une page — un corps qui **est** un tableau JSON ne donnait rien, `sliceJsonLiterals` n'isolant que ce qui suit un « = » ou un « : » ; et `URL_KEYS` était comparée à l'identique, si bien que `embedUrl`, `streamUrl` ou `videoSrc` (la façon dont les API nomment leurs adresses) n'étaient jamais reconnues. `isUrlKey` accepte désormais ces clés suffixées ; les images ainsi ramassées (`badgeUrl`) restent écartées par le pointage.
  - **Lien mort supprimé** : la source liste des matchs pour lesquels elle n'a aucun flux (vérifié, l'API renvoie `[]`). Ils recevaient en repli l'adresse de l'API elle-même — **31 matchs de football universitaire** portaient ainsi un lien qui n'affiche que « [] ». `isApiEndpoint` coupe ce repli des deux côtés (script serveur et `scrapeMatchFlux`).
  - Le football américain arrive dans une seule catégorie, universitaire et professionnel mêlés. La ligue se lit d'abord dans l'identifiant du flux (`live_cfb_`, `live_nfl_`) ; à défaut, `leagueOfTeamName` (nouveau, `js/db.js`) interroge la base d'équipes existante, qui recense les 32 clubs de la NFL et non les centaines d'équipes universitaires — un nom qu'elle ignore est universitaire. Relevé sur l'API réelle : **CFB 97, NFL 3**.
  - `tests/unit_streamed.test.js` (nouveau, 9 groupes, sans réseau).

- 2026-09-04 - **Vue « À venir » retirée.** Elle n'était qu'une liste à plat du programme de la journée, que le Guide couvre déjà sous forme de grille temporelle. Bouton retiré de `index.html`, branche de rendu retirée de `buildEPGInner`, et `'upcoming'` sorti des trois listes de vues de `js/utils.js` (rassemblées en une seule constante `FILTERS`, qui était recopiée à l'identique à trois endroits).
  - `applyFilter` retombe sur le direct quand on lui passe une vue inconnue. Sans cette garde, un appel résiduel — raccourci mémorisé, ancien code — laissait `data-filter` sur une valeur qu'aucune branche de rendu ne traite : page vide, sans même un message. Le test le vérifie, et échoue bien si on retire la garde.

- 2026-09-04 - **Onglet Live : fenêtre stricte, et les toggles qui ne fonctionnaient pas.**
  - **La fenêtre.** L'onglet devait contenir le direct et l'heure qui vient ; il affichait, un soir à 19:34, **plus de 200 matchs pour 2 réellement en cours**. Le prédicat gardait tout ce qui avait commencé dans les 24 dernières heures (`diff > -1440`) : 128 matchs passaient à ce titre, dont un de 01:00 le matin même, plus 50 sur un statut `live` périmé (le plus ancien commencé 19 h plus tôt). La cause de fond est que `startTime` est une heure du jour sans date, et que le rattrapage de minuit qui l'accompagnait (`if (currentMins >= 1380 && mMins <= 60)`) ne couvrait que la fenêtre 23:00 → 01:00.
  - Or les matchs portent un `matchDate`. `minutesUntilStart` (js/config.js) s'en sert et rend un écart signé exact ; le décalage de jour se calcule sur les dates civiles EST, sans que l'heure d'été n'intervienne puisqu'on ne prend qu'une différence de jours entiers. `isLiveNow` et `startsWithin` s'appuient dessus, avec une borne de durée plausible (`LIVE_MAX_DURATION_MIN`, 4 h) qui écarte les statuts périmés. La même règle sert à la pastille « is-live » de la barre de navigation, qui recopiait le calcul et pouvait donc contredire la liste.
  - Section « Plus tard aujourd'hui » supprimée, et « Favoris aujourd'hui » renommé « Favoris » : le titre aurait menti, la section ne pouvant plus contenir que des favoris en cours ou imminents. Relevé après correction : « Autres streams » passe de 157 à 58 matchs.
  - **Les toggles.** Aucun titre de section ne se repliait — 8 sur 8 sans effet, avec une exception JavaScript à chaque clic. `renderMatches` déclarait `var icon` pour le chevron, puis **une seconde fois dans la même portée de fonction** pour l'icône de ligue. Au clic, le gestionnaire trouvait une chaîne (ou `undefined`) à la place de l'élément, levait « Cannot set properties of undefined » **avant** la ligne qui masque la grille : l'état basculait, l'affichage jamais. Un test lisant `S.collapsedSections` aurait été vert.
  - Corrigé (`chevron` / `leagueIcon`), et la rotation passe dans `styles.css` plutôt que dans un `style.transform` posé par JavaScript — c'est ce couplage qui avait rendu la panne totale.
  - UX reprise dans la foulée : les sections de premier niveau (Favoris, Live, À venir) sont repliables comme les autres, l'onglet ne mêlant plus des titres qui réagissent au clic et d'autres non ; chaque titre repliable est un vrai bouton (`role`, `tabindex`, `aria-expanded`, Entrée et Espace) ; le bouton « Tout voir » disparaît quand la section est repliée, où il n'aurait rien à déplier.
  - `renderGroupedSection` construit désormais ses sous-groupes même repliée. Auparavant elle sortait avant : la section gardait la grille à plat, si bien que la déplier montrait les matchs en vrac au lieu des groupes par ligue — et rien ne pouvait le corriger puisque le clic ne re-rendait pas.
  - **Tests.** `tests/unit_livewindow.test.js` (nouveau, 10 groupes, heure de référence fixée donc indépendant du moment où il tourne) : écart signé, passage de minuit dans les deux sens, occurrence la plus proche sans date, `null` plutôt que zéro sur une entrée illisible, borne de durée, et l'absence de chevauchement entre `isLiveNow` et `startsWithin` — sans quoi un match apparaîtrait dans deux sections. Deux tests de démarrage : chaque section repliable atteignable répond au clic **et au clavier** sans exception, et l'onglet ne contient que le direct et l'heure qui vient.
  - **Fragilité introduite par la correction, et refermée** : en restreignant la fenêtre, les tests de démarrage se sont mis à dépendre de l'heure à laquelle la suite tourne — une exécution à 4 h du matin trouve une grille vide et fait échouer des tests sans rapport. `bootOffline` fige donc l'horloge du navigateur (`page.clock.setFixedTime`) sur le `generatedAt` de `data/streams.json`, relu dans le fichier puisqu'il est régénéré chaque heure. Vérifié en décalant l'horloge de 9 h : **8 tests sur 10 échouent** sans le figeage, aucun avec. L'en-tête du fichier promettait un test déterministe ; le temps en fait désormais partie.
  - Les deux tests ont été éprouvés à l'envers : en réintroduisant l'ancien prédicat, celui de la fenêtre détecte **223 matchs hors fenêtre**. Le premier jet du même test cherchait `dataset.matchId`, qui n'existe pas (les cartes portent `id="mb-<id>"`) : il passait sans rien vérifier, d'où la garde qui échoue maintenant si plus aucune carte ne peut être reliée aux données.

- 2026-09-03 - **Le blocage des popups passe du script Tampermonkey au site.** Question posée : quelles fonctions du script peuvent rejoindre l'application sans se casser ? La ligne de partage est que manipuler le DOM d'une page tierce exige l'extension (origine croisée), mais ce que le navigateur applique de façon déclarative peut vivre dans le site.
  - `injectPopupBlocker` écrasait `window.open` et interceptait les clics `target="_blank"` **à l'intérieur** de la page tierce. L'attribut `sandbox` obtient le même résultat depuis l'application : `PLAYER_SANDBOX` (`js/embed-bridge.js`) est désormais posé sur l'iframe du lecteur, avant toute affectation de `src` — l'attribut n'est lu qu'au chargement du document.
  - Mesuré au navigateur sur un faux lecteur hostile servi en origine croisée, **avec un vrai clic** (Chrome bloque déjà la navigation du parent sans activation utilisateur, si bien qu'un test sans clic passe au vert même sans bac à sable et ne prouve rien) : **sans sandbox, le clic détourne l'onglet entier et ouvre un popup ; avec, ni l'un ni l'autre.** Et rien de ce dont un lecteur a besoin ne casse — `localStorage`, requêtes vers son propre domaine, `<video>`, plein écran, tout reste vert grâce à `allow-same-origin`, sans danger ici puisque l'iframe est d'origine croisée.
  - Le document **reconstruit** garde `EMBED_SANDBOX`, sans `allow-same-origin` : celui-là vit à notre origine, le lui accorder lui ouvrirait le `localStorage` de l'application. Ce bac à sable-là ne dépend pas de la préférence.
  - Réglage **Options → Réseau & Proxys → « 🛡️ Bloquer popups et redirections des lecteurs »** (activé par défaut), pour le cas où un lecteur précis refuserait de démarrer.
  - Deux tests : le différentiel sur lecteur hostile (qui vérifie d'abord que l'attaque réussit **sans** bac à sable, sinon il ne prouverait rien), et l'inventaire des jetons — `allow-popups`, `allow-top-navigation*` et `allow-modals` doivent rester absents, un seul ajouté par mégarde rouvrirait la porte.
  - Ce qui ne peut **pas** quitter l'extension, et pourquoi, est consigné dans `docs/ARCHITECTURE.md` : le nettoyage visuel lit le DOM du site tiers, et le pont `GM_xmlhttpRequest` n'existe que par le privilège d'extension.

- 2026-09-03 - Suite de la revue du tour de passe-passe : **le lecteur est extrait de la page avant toute reconstruction**.
  - Constat qui a déclenché la correction : ma mise en garde initiale (« la page fabrique son lecteur depuis une adresse chiffrée ») décrivait un cas rare. Croisement des 674 liens « page » de `data/streams.json` avec `hostPolicy` (que le script serveur remplit en lisant réellement les en-têtes) : **294 (43 %) sont refusés par `X-Frame-Options`/`frame-ancestors`, 242 (35 %) par un 403/429 Cloudflare** — que le canal script lève précisément — et 138 (20 %) viennent d'hôtes non sondés. Le chiffrement ne concerne qu'une source sur huit, VIPLeague et son `stream.bun.min.js` obfusqué.
  - Conséquence : ces 674 liens sont des **pages de match d'agrégateurs**, dont tout l'intérêt est le lecteur tiers en `<iframe>` à l'intérieur — et ces hôtes de lecteurs acceptent l'encadrement (`hostPolicy` : `embedsports.me`, `streame.center`, `tnt-usa.biz`, `dudestream1.com`… tous `embeddable: true`). Reconstruire la page entière en origine opaque était donc le mauvais premier réflexe : `pickEmbeddablePlayer` (`js/embed-bridge.js`) passe désormais le HTML téléchargé à `extractPlayers` (`js/extractors.js`, déjà écrit et testé) et charge le lecteur trouvé dans une **iframe ordinaire** — vraie origine, cookies, référent, aucun bac à sable. La reconstruction en `srcdoc` devient le repli, pour les pages qui ne livrent pas de lecteur. Un candidat hébergé sur la page elle-même est écarté : il retomberait sur l'en-tête qui a bloqué au départ.
  - Le badge du lecteur distingue les deux issues : « 🎩 lecteur » (vert, lecture normale) et « 🎩 page » (neutre, affichage possiblement partiel).
  - **Défaut trouvé en vérifiant** : le plancher de 200 caractères s'appliquait *avant* l'extraction, si bien qu'une page de match courte était jetée avec son lecteur. Le plancher ne sert plus qu'à décider si une réponse **sans lecteur** vaut la peine d'être reconstruite (`MIN_REBUILDABLE_LENGTH`) — sinon un fragment d'erreur de proxy donnait une iframe vide sans que l'utilisateur sache que le tour avait échoué.
  - **Flake de test corrigé au passage** : `window.hasLoadedOnce` est posé dans le `.finally` de `loadAll`, mais le rendu atterrit dans le DOM **50 à 150 ms plus tard** (mesuré sur six démarrages : cinq fois sur six, la grille était encore vide au moment où le test lisait). Les assertions sur les cartes des tests existants ne passaient donc que par chance. `bootOffline` (`tests/test_app_boot.spec.js`) attend désormais les cartes elles-mêmes ; leur absence reste une vraie erreur, signalée par le dépassement de délai. Suite relancée trois fois de suite : 6/6 à chaque fois.
  - `js/embed-bridge.js` n'importe plus rien d'autre que `js/extractors.js`, lui-même hors de tout cycle. Six groupes de tests de plus (`tests/unit_links.test.js`, 16 au total). Vérifié au navigateur sur une page d'agrégateur simulée : le lecteur extrait est chargé sans `sandbox`, badge « lecteur », aucun bandeau d'échec.

- 2026-09-03 - Interface mobile en affiches verticales, liens externes jouables dans le Multivision, recherche des liens manquants et inventaire par domaine primaire.
  - **Affiche verticale (« à la Netflix »), `styles.css` + `js/ui.js`.** La carte large (2,4 : 1) occupait toute la largeur d'un téléphone pour un seul match : trois tenaient à l'écran de 390 px, lire la suite du programme demandait un défilement interminable. Une classe `cards-poster` sur `<body>` (posée par `applyCardShape`, automatiquement sous 900 px ou forcée par Options → Style des Cartes → Forme des cartes) fait passer chaque section en **rail horizontal d'affiches 2 : 3** : une dizaine de matchs à portée de pouce sans repousser les sections suivantes hors de vue, titres de section collants, bouton « Tout voir » pour déplier un rail en grille. **Le HTML des cartes est inchangé** — tout se joue en CSS, il n'y a donc pas deux générateurs de cartes à maintenir. Couvert par deux tests Playwright : la vignette est bien en portrait, le rail défile dans sa section et non dans la page (le piège d'un `overflow-x` mal placé), le bouton de dépliage existe.
  - **Liens externes dans le Multivision, Firefox compris (`js/embed-bridge.js`, `multiview-cleaner.user.js` 1.2).** Une page qui répond `X-Frame-Options: DENY` est refusée par le navigateur *avant* tout JavaScript : aucun attribut d'iframe ne peut lever ce refus, dans Firefox comme ailleurs. Le seul contournement possible est de ne pas laisser le navigateur charger l'adresse dans l'iframe : on récupère le HTML par un canal que cet en-tête ne régit pas, puis on le pose via `srcdoc`, où il n'y a plus d'adresse distante donc plus d'en-tête à faire respecter. Deux canaux, dans l'ordre : le script utilisateur (nouveau pont `GM_xmlhttpRequest` ↔ `postMessage`, qui passe là où les proxys se font refouler par Cloudflare), puis les proxys CORS de `fetchPage`. Le document reconstruit est posé en **origine opaque** (`sandbox` sans `allow-same-origin`) : il ne peut lire ni le stockage ni le DOM de l'application — plus strict que l'iframe normale du Multivision ; une cale lui fournit en échange un `localStorage` factice et fait taire les gardes anti-encadrement. Réglage : Options → Réseau & Proxys → « Reconstruire les pages non intégrables » (activé par défaut), état du pont affiché dans la même section. **Remplace** l'ancien bandeau « Si l'embed est bloqué (Firefox / X-Frame) : Ouvrir / Script », qui ne faisait que constater l'échec ; ce bandeau ne subsiste que comme repli, quand aucun canal n'aboutit.
  - **Recherche des liens manquants (`js/links.js`).** Le badge d'une carte sans flux, jusqu'ici absent, est devenu le bouton 🔎 qui lance la recherche pour ce match (`cardSearchLinks`) — il fallait auparavant ouvrir chaque fiche une par une pour déclencher `scrapeMatchFlux`. Bouton « 🔎 Liens manquants » dans la barre d'outils du Guide : balaie en série les matchs à venir sans lien (plafonné à 12, les plus proches d'abord), avec barre d'avancement.
  - **Nombre de liens par domaine primaire (`js/links.js`, page Logs).** `getDomain` rend le nom d'hôte complet : `embed1.exemple.com` et `embed2.exemple.com` comptaient pour deux fournisseurs. `primaryDomain` replie les sous-domaines sur le domaine enregistrable (suffixes à deux niveaux gérés). L'inventaire trié affiche liens, liens intégrables, liens « page », matchs couverts et sous-domaines vus par domaine, plus le décompte des matchs sans lien et de ceux qui sont relançables. Relevé sur le cache du jour : 1079 liens, 62 domaines, `embedsports.me` 138 liens tous intégrables contre `onhockey.tv` 138 liens tous en pages, `sportsurge.net` et `soccersurge.io` à 124/62 chacun (miroirs l'un de l'autre). Dans la fiche d'un match, la même répartition sert de **filtre** : sur 38 liens venant de 28 domaines, une pastille par domaine ramène la liste à ce qu'on cherche. Les six premiers domaines seulement ont leur pastille, la queue est repliée sous « Autres (n) » — en plusieurs lignes, la rangée repoussait la liste des flux sous le bas de l'écran.
  - **Nettoyage des versions précédentes.** Le compteur de flux d'une carte existait en double : la pastille `.card-streams` posée au rendu (en haut à droite) et `.prime-stream-count` ajoutée par `updateMatchUiAfterScrape` (en bas à droite) affichaient la même chose au même moment ; une seule subsiste, et un test vérifie qu'il n'y en a jamais deux. Corrigé au passage « 2 fluxs » (flux est invariable). Le style en ligne des titres de section, recopié à chaque appel dans `js/ui.js`, passe en classe `.section-title`.
  - Fichiers : `js/links.js` et `js/embed-bridge.js` (nouveaux, **sans aucun import** — comme `js/fetcher.js` et `js/extractors.js`, pour ne pas ajouter d'arête au cycle `utils ↔ config ↔ ui ↔ multiview ↔ main ↔ scrapers ↔ api`), `js/ui.js`, `js/multiview.js`, `js/scrapers.js`, `js/state.js`, `styles.css`, `index.html`, `multiview-cleaner.user.js`, `sw.js` (`CACHE_NAME` → `sports-guide-v4`, deux modules ajoutés à la coquille), `package.json`.
  - Tests : `tests/unit_links.test.js` (nouveau, 10 groupes : domaine enregistrable, agrégation, matchs relançables, document reconstruit, bac à sable sans `allow-same-origin`) et deux tests Playwright de plus dans `tests/test_app_boot.spec.js`. `npm test` : 9 fichiers unitaires + 6 tests de démarrage, tous verts.
  - Limite connue, à ne pas promettre à l'utilisateur : si une page ne contient pas son lecteur mais le fabrique depuis une adresse chiffrée ou un appel authentifié à son propre domaine, le document reconstruit reste vide. Le repli « Ouvrir dans un onglet » est alors proposé.
- 2026-09-03 - Revue de code et fiabilité du démarrage. **L'application ne rendait plus aucun match** : `getLogo` (js/db.js) levait `colors[1].replace of undefined` pour toute équipe sans logo dont les couleurs venaient d'une des 25 entrées de `TEAM_DATA` n'en déclarant qu'une (sélections nationales surtout, atteintes par la correspondance floue de `getTeamColors`). L'exception traversait `buildEPG` → `#marea` restait vidé, `#ov` et `#errbox` (qui y vivent) étaient perdus, puis le `.finally` de `loadAll` plantait à son tour sur `document.getElementById('ov').style` : `window.hasLoadedOnce` n'était jamais posé et l'événement `loadSequenceComplete` jamais émis, donc **ni actualisation automatique des scores (60 s), ni défilement « Maintenant », ni boîte d'erreur / bouton « Réessayer »**. Écran final : « Aucun match en direct pour le moment » avec 119 matchs chargés en mémoire.
  - `js/db.js` : `teamColorPair()` complète toujours la paire fond/texte ; `getLogo` l'utilise.
  - `js/ui.js` : `buildEPG` enveloppe `buildEPGInner` et restaure `#ov`/`#errbox` si le rendu échoue ; chaque carte de match est construite dans un `try/catch` (données agrégées : un match malformé est ignoré, journalisé, et n'efface plus la grille). Alias `logLine` car la variable locale `lg` (objet ligue) masque la fonction de journal `lg`.
  - `js/main.js` : `hideLoadingOverlay()` / `showLoadError()` remplacent les six accès directs à `#ov`/`#errmsg`/`#errcode`/`#errbox`, qui supposaient tous que ces éléments existent encore.
- 2026-09-03 - Deux requêtes sans délai maximal pouvaient bloquer le démarrage indéfiniment (réseau qui avale la requête au lieu de la refuser : portail captif, pare-feu, mandataire mal configuré). Mesuré : aucun rendu après 40 s, contre 27 s avec le correctif.
  - `js/config.js` : `fetchRemoteConfig()` (attendu tout au début du premier chargement) reçoit un `AbortController` de 5 s — `REMOTE_CONFIG_TIMEOUT_MS`.
  - `js/api.js` : la requête TheSportsDB était la seule du `Promise.all` du calendrier sans `AbortSignal.timeout` ; alignée sur les 8 s des appels ESPN/LoL.
- 2026-09-03 - Dégradation gracieuse quand ESPN est injoignable. `mergeFluxToApi` (js/api.js) forçait `league = 'Autres Flux'` sur **tout** flux non apparié : sans réponse d'ESPN, les 119 matchs (NFL, MLB, NBA, Champions League…) basculaient dans une section repliée absente de la grille temporelle du Guide, alors que `data/streams.json` portait le bon nom de ligue. Le nom est désormais conservé, mais uniquement si l'API ne renvoie **aucun** match pour cette ligue — sinon un échec de fusion produirait un doublon visible, contraire au principe API-First. Vérifié hors ligne : NFL 15, MLB 12, NBA 4, Champions League 6 au lieu de « Autres Flux » 119.
- 2026-09-03 - Service worker (`sw.js`, `CACHE_NAME` → `sports-guide-v3`). Trois défauts :
  - **Cache sans fin** : `data/streams.json?t=…` et `data/schedule.json?t=…` changent d'adresse à chaque chargement ; chaque passage ajoutait une entrée (mesuré : +1 par rechargement, indéfiniment) et le repli hors ligne ne retrouvait jamais l'entrée précédente. Les requêtes de même origine sont rangées sous leur adresse sans chaîne de requête (mesuré : 20 entrées stables sur quatre rechargements).
  - **Réponses invalides mises en cache** : seules les requêtes GET dont la réponse est `ok` et `basic` sont stockées (`cache.put` refusait les autres méthodes et rangeait les 404).
  - **Coquille incomplète** : seuls `index.html` et `manifest.json` étaient pré-chargés ; l'application découpée en modules ne pouvait pas démarrer hors ligne. Les 20 fichiers de la coquille (CSS, 14 modules JS, données) sont pré-chargés un par un pour qu'une ressource absente ne fasse pas échouer l'installation. Vérifié : serveur coupé + navigateur hors ligne → 119 matchs, 14 cartes.
- 2026-09-03 - Tests. Aucun test ne vérifiait que l'application démarre : le plantage ci-dessus passait les huit fichiers de tests unitaires et l'intégration continue.
  - `tests/test_app_boot.spec.js` (nouveau, Playwright, **réseau externe entièrement coupé** — donc déterministe, contrairement aux specs qui visitent les sites sources) : démarrage, présence de cartes, survie de `#ov`/`#errbox`, bouton « Réessayer », rotation des huit onglets, absence de débordement horizontal à 390 px. Vérifié : échoue sur le code d'avant, passe après.
  - `tests/unit_merge.test.js` (nouveau, 6 groupes) : fusion sans doublon, classement des flux non appariés dans les quatre cas (ligue couverte / absente / API muette / ligue inconnue), filtres TBD-équipes vides-matchs terminés préservés.
  - `tests/getLogo.test.js` : deux cas d'équipe à une seule couleur + vérification de `teamColorPair`.
  - Les deux nouveaux fichiers sont branchés dans `npm test`.
- 2026-09-03 - `js/multiview.js` : la palette d'un favori était silencieusement ignorée pour les 25 équipes à une seule couleur (`colors.length >= 2`) ; passe par `teamColorPair`.

- 2026-09-03 - Traitement des six constats laissés à l'arbitrage lors de la revue précédente.
  - **Onglet « À venir » rendu atteignable.** `applyFilter('upcoming')` et sa branche de rendu dans `buildEPG` fonctionnaient, mais aucun bouton ne les appelait : la vue liste de tous les matchs du jour (par opposition à la grille temporelle du Guide) était inaccessible. Bouton `#filter-upcoming` ajouté entre Live et Guide (`index.html`). Couvert par un test qui clique les vrais boutons de la barre de navigation, pas `applyFilter` (`tests/test_app_boot.spec.js`) — il échoue bien si le bouton disparaît.
  - **Six fonctions mortes supprimées** (déclaration, export et liaison `window.*`) : `isMatchContext` (`js/match.js`, simple enveloppe autour d'`isMatch`), `clearMultivision` et `hideMultivision` (`js/multiview.js`), `findLeagueHeader` et `getEstTime` (`js/scrapers.js`), `filterBuggyMatches` (`js/api.js`, devenue un `return true` pour tout). Aucune référence restante dans `js/`, `index.html`, `scripts/` ni `tests/`. `SUIVI_FONCTIONS.md` en est purgé et porte désormais un avertissement : il décrit encore un `app.js` unique disparu depuis le découpage en modules, ses listes « Appelle / Appelée par » ne sont plus fiables.
  - **17 fichiers jetables supprimés à la racine**, interdits par `AGENTS.md` : `patch_api.js`, `patch_api_fix.js`, `patch_bindings.py`, `patch_fetchpage.py`, `patch_index.py`, `patch_investigator.py`, `patch_investigator2.py`, `patch_js_module.html`, `parse_wwe_test.html`, `css_patch{,2,3}.patch`, `parse_wwe.patch`, `update_api.patch`, `verification_screenshot{,2}.png`, `plan.md`. Vérifié : aucun fichier de code ne les référençait.
  - **Surveillance des domaines séparée des tests.** `tests/test_scrapers.spec.js` (que *aucun* script n'exécutait) et `tests/test_scrapers_extract.spec.js` recopiaient chacun leur liste de domaines, déjà désynchronisée du code — VIPLeague y était testé sur `vipleague.vg/` alors que l'application lit `vipleague.vg/live-now-streaming`. Les deux sont remplacés par `tests/test_domains.spec.js`, qui lit `SCRAPERS_CONFIG` et `SOURCE_MIRRORS` dans `js/config.js` (via jsdom) et essaie l'adresse principale puis les miroirs, comme l'application. Ce n'est pas un test de non-régression mais une surveillance : elle dépend de l'adresse IP d'exécution (Cloudflare répond 403/429/520 aux centres de données) et rendait rouges des changements de code sans rapport. Elle sort donc de `npm test` vers `npm run test:domains`, exécuté quotidiennement par `.github/workflows/domains-watch.yml` (plus déclenchement manuel). `npm test` ne contient plus que du déterministe : 8 fichiers unitaires + `tests/test_app_boot.spec.js`.
  - **Cycles d'imports réduits de moitié.** 20 imports référencés nulle part retirés (détection conservatrice : une occurrence dans une chaîne de caractères compte comme une utilisation, donc aucun import réellement utilisé n'a pu être supprimé), et deux appels qui passaient par `window.*` alors que la fonction était déjà importée rendus directs (`setApiTargetDate` dans `js/main.js`, `loadAll` dans `js/multiview.js`). Résultat mesuré : 673 cycles au lieu de 1298, et surtout **`js/db.js` et `js/match.js` sortent de tout cycle** (`db.js` ne dépend plus que de `teams.js`) — ce sont les modules qui portent `normName`, `getLogo` et `leagueTier`, appelés partout. Corrigé au passage un plantage d'ordre d'évaluation que ce travail a mis au jour : `initPrefs()`, appelé au chargement de `js/multiview.js`, lisait `userPrefs` (exporté par `js/ui.js`) et plantait sur `undefined` selon le module par lequel on entrait dans le graphe — en production `main.js` est le point d'entrée et l'ordre était bon, mais tout import de `js/config.js` depuis un script ou un test échouait. `initPrefs` et `applyBgStyle` tolèrent désormais l'absence de `userPrefs`. **Le nœud central subsiste** (`utils ↔ config ↔ ui ↔ multiview ↔ main ↔ scrapers`) : il demande une vraie restructuration, pas un nettoyage d'imports.
  - **`getTeamColors` ne reparcourt plus les 821 équipes à chaque appel.** Les clés normalisées sont calculées une fois (l'ordre d'origine est préservé, donc la première correspondance trouvée reste la même) et les résultats sont mémorisés — `TEAM_DATA`, `TEAM_ALIASES` et `NORM_TEAM_KEYS` sont figés après le chargement du module. Vérifié sur 7346 noms (toutes les clés, tous les noms et alias de `TEAM_DATA`, plus les équipes réelles de `data/streams.json` et des variantes bruitées) : **0 différence** avec l'implémentation précédente. Scénario réaliste (250 noms de la grille du jour, 20 passes de rafraîchissement, 5000 appels) : **155 ms → 0 ms**. Couvert par `tests/getLogo.test.js`.

- 2026-09-03 - Corrige un flux attaché au mauvais match, signalé fréquent : `TEAM_ALIASES` (js/db.js) écrasait silencieusement l'alias d'une équipe par celle déclarée après elle dans teams.js (353 alias partagés par 2+ équipes distinctes) — un match « Falkirk vs Rangers » (Scottish Premiership) ressortait avec « Texas Rangers » (MLB) comme adversaire, l'alias "rangers" gagné par la dernière équipe du fichier. Un alias réclamé par plusieurs équipes est désormais retiré de la table plutôt que résolu au hasard ; les alias propres à une seule équipe et les noms complets continuent de fonctionner normalement. `tests/unit_teamAliases.test.js` (6 tests). Corrige aussi le clic sur un flux non intégrable (« page ») qui ouvrait un nouvel onglet automatiquement (`js/multiview.js: openFlux`) — sur certains navigateurs mobiles ça n'ouvrait qu'un onglet vide ; tout passe maintenant par le Multivision comme les autres flux, le bouton ↗ dédié restant le moyen explicite de sortir de l'application.
- 2026-09-03 - Corrige un crash OnHockey (`isMatchPair` non importé dans `js/scrapers.js`, exposé en pointant OnHockey vers sa vraie grille) et deux comptages de flux par source erronés dans `scripts/scrape_streams.mjs` (attribution au match découvreur plutôt qu'au domaine réel du lien) qui faisaient croire que plusieurs sources ne donnaient aucun flux.
- 2026-09-03 - Extraction de flux générique (`js/extractors.js`, `tests/unit_extractors.test.js`) : au lieu d'une branche `if (site === x)` par agrégateur, un moteur récolte les candidats par stratégie (iframe, bouton data-*/onclick, blob JSON, adresse encodée, lien externe) puis les note, en apprenant quels hôtes acceptent l'affichage intégré (registre persistant `embed_registry`, alimenté par `scripts/scrape_streams.mjs` qui lit réellement les en-têtes X-Frame-Options/CSP et par un retour utilisateur côté lecteur). Branché en complément dans `extractStreamLinks` (les branches par site existantes gardent leurs libellés). Corrige aussi : les pages non intégrables (`topLevel`) ouvrent maintenant un onglet au clic au lieu de partir dans l'iframe (écran « Firefox ne peut pas ouvrir cette page ») ; OnHockey pointe ses matchs vers `schedule_table.php` au lieu de l'accueil (0 page relue jusqu'ici) ; cinq bugs francs corrigés (liaisons `window.*` mortes dans `rebuildProxies`, assignation à un import ES dans `openGlobalStatsFromMatch`, `var` partagée dans `fetchSubPages`, boutons `closeDbg`/`gstatsGoBack` sans fonction, fuseau local au lieu de l'Est pour PWHL/WWE/LoL) ; purge du cache localStorage par date, jamais vidé jusqu'ici.
- 2026-09-02 - Allègement : une seule voie de mise à jour des liens, requêtes inutiles supprimées.
  - Constat mesuré sur une exécution serveur complète : **376 requêtes échouées sur 562** (67 %). `footybite.bid` échouait **58 fois sur 58** (403 Cloudflare sur `/game/`), les miroirs Streameast **14 fois sur 15** (429). Côté navigateur, chaque chargement lançait le scraping en direct des huit sources puis le pré-scraping d'une centaine de pages de match, le tout via proxys CORS à 3-8 s de délai.
  - **Le cache serveur devient la source principale.** `data/streams.json` agrège déjà les huit sources chaque heure, sans proxy et de façon déterministe. Le scraping en direct des pages de liste et le pré-scraping au démarrage ne s'exécutent plus que si ce cache manque, est vide ou dépasse trois heures (workflow GitHub en panne). Le scraping d'un match précis reste déclenché à l'ouverture de sa fiche.
  - `MATCH_PAGE_BLOCKED_HOSTS` / `isMatchPageBlocked` (`js/config.js`) : Footybite et les miroirs Streameast ne sont plus téléchargés (serveur comme client), mais leur lien reste proposé — ces pages s'ouvrent normalement dans le navigateur de l'utilisateur, via le bouton ↗. `matchPageFallbackLink` factorise ce lien.
  - Sous-pages mortes retirées : `methstreams.gs/league/nhlstreams` et `/league/cfbstreams` redirigent vers crackstreams.mx qui répond 404.
  - Résultats : requêtes serveur **376 échecs → 10**, pages de match **100 % de réussite** (footybite 72/72, buffstreams 64/64, vipleague 20/20). Dans le navigateur : **0 requête via proxy, 0 vers les sites sources**, premier rendu en 3,8 s, 136 matchs et 176 liens affichés.
  - Données finales : 181 liens, 0 doublon, 0 page d'index, 0 libellé parasite, provenance renseignée sur 181/181 (contre 104/161), qualité honnête (150 inconnues, 31 réellement annoncées) au lieu de « HD » partout.
- 2026-09-02 - Audit du système de liens (association, doublons, métadonnées).
  - Constat sur les données réelles : 4 doublons dans un même match, 5 adresses attachées à plusieurs matchs, 8 liens pointant sur `ms.buffstream.io/index-version-27` (l'accueil du site, pas un flux), 3 libellés parasites. 138 liens sur 151 en langue « MULTI », 90 avec un nom générique, qualité « HD » sur presque tout car c'est le défaut des parseurs.
  - Cause : le nettoyage et l'enrichissement n'existaient que dans `extractStreamLinks`. Les liens construits directement par `parseOnHockey` et `parseStreameast` n'y passaient pas.
  - `finalizeStreamLinks` (`js/scrapers.js`) devient l'entonnoir unique : écarte les pages d'index (`isIndexPageUrl`) et les libellés parasites (phrases parasites n'importe où, mots de navigation en tête seulement), dédoublonne sur l'adresse normalisée (`normalizeStreamUrl` : protocole, `www.`, barre finale, casse), puis renseigne site / chaîne / qualité / langue via `describeStreamLink` (35 chaînes reconnues, les plus spécifiques d'abord). Appelé aussi par le script serveur.
  - Qualité : les parseurs mettaient « HD » ou « SD » par défaut ; la valeur n'est gardée que si l'adresse ou le libellé l'annonce. Le badge est masqué sinon.
  - Interface : chaque ligne de flux affiche la chaîne, le site et la langue sous son nom.
  - Clic sur une ligne : n'ouvre plus jamais un onglet externe (c'était imposé aux liens `topLevel`). Un bouton ↗ dédié, disponible pour tous les flux, est ajouté à côté du bouton Multivision.
  - Tests : `tests/unit_scrapers.test.js` passe de 8 à 13 groupes.
- 2026-09-02 - Audit des sept appels de chargement. Deux défauts corrigés :
  - **Cache serveur jamais relu pendant une session** : la condition `!window.prefetchedStreamMatches || !isBackground` ne relisait jamais `data/streams.json` lors d'une passe d'arrière-plan, la variable restant définie (même vide) après le premier chargement. Une session ouverte plusieurs heures restait sur les liens du démarrage alors que le fichier est régénéré chaque heure. Relecture forcée dès qu'il a plus de 10 minutes (`prefetchedStreamsLoadedAt`).
  - **Bouton « Réessayer »** : `loadAll(window.hasLoadedOnce, true)` valait `loadAll(true, true)` après le premier chargement, donc une passe d'arrière-plan sans réinitialisation d'état ni relecture du cache. Devient `loadAll(false, true)`.
  - Le premier chargement sans cache passait `window.hasLoadedOnce` (valeur `undefined`) : remplacé par `false` explicite.
  - Les cinq autres appels sont corrects. Vérifié dans Chromium : l'horodatage du cache se rafraîchit sur une passe périmée et reste inchangé sur une passe fraîche. Note pour les tests futurs : le service worker intercepte `data/streams.json`, donc compter les requêtes via `page.route` ne voit pas ces lectures.
- 2026-09-02 - Nombre de flux instable d'un rechargement à l'autre : six déclencheurs appellent un chargement complet (démarrage, minuterie de 5 min, changement de date, « Réessayer », réglages réseau, rechargement du cache serveur) et rien n'empêchait deux passes de se chevaucher. Chacune fusionne ses liens dans la même grille, d'où un compte qui bougeait selon l'ordre d'arrivée des proxys. `loadAll` (`js/main.js`) est scindé en une garde et `loadAllRun` : une passe d'arrière-plan lancée pendant qu'une autre tourne renvoie la promesse en cours au lieu de refaire le travail ; une passe premier plan ou forcée reste toujours autorisée. Vérifié dans Chromium : cinq passes simultanées se replient sur une seule (146 flux avant et après), premier plan non bloqué.
  - Reste inhérent au fonctionnement : le nombre de liens grandit pendant une session à mesure que le scraping d'arrière-plan répond, et varie d'un rechargement à l'autre selon les proxys CORS qui répondent dans le délai. Le cache serveur, lui, est déterministe.
- 2026-09-02 - Fenêtre de match : l'en-tête était vide (rien n'indiquait la rencontre ni la compétition ouverte) → icône, ligue et affiche du match. « 0:00 » sous le score remplacé par « DIRECT » via `formatLiveMinute` (`js/ui.js`), partagé avec les cartes.
- 2026-09-02 - Interface : densité des cartes et informations de décision.
  - Cartes de match : la vignette 16/9 mangeait ~210 px pour deux logos → ratio 2.4/1 (`--card-aspect`), hauteur de carte 257 → 202 px.
  - Informations ajoutées sur la carte : **nom de la ligue** (l'icône seule ne disait pas de quelle compétition il s'agissait) et **nombre de flux disponibles** (`▶ N`), qui décide du clic.
  - Statut en direct : « 0:00 » renvoyé par certaines API est remplacé par « DIRECT ».
  - Contrôles de zoom (« Maintenant », ±) : ils flottaient au-dessus des cartes dans En direct et À venir sans aucun effet, et masquaient l'heure des matchs sur mobile. Ils sont désormais réservés à la grille temporelle du Guide (`body.view-timeline`), la visibilité étant portée par le CSS et non plus par un style en ligne.
  - Vérifié dans Chromium : 1 colonne à 360/390 px, 2 à 768, 3 à 1024/1440/1920, aucun débordement horizontal, aucune erreur JS, contrôles de zoom visibles uniquement dans le Guide.
- 2026-09-02 - Ligues : classement principal/secondaire/ignoré réglable, séparation dans l'interface, couverture des sources.
  - `js/db.js` : `leagueTier(league, overrides)` et `defaultLeagueTier` — source de vérité unique du niveau d'une ligue. Retrait de `FIFA WORLD CUP` d'`OTHER_LEAGUES` (inatteignable : l'alias la ramène à `WORLD CUP`, déjà principale).
  - `js/state.js` : `leagueTiers` (localStorage `league_tiers`), `setLeagueTier`, `resetLeagueTiers`. Le choix de l'utilisateur prime sur les listes par défaut.
  - `js/ui.js` : trois niveaux dans En direct, À venir et le Guide. Les ligues secondaires ont leur propre section (et leur propre grille temporelle dans le Guide), les ignorées disparaissent partout. Nouvel assistant `renderGroupedSection` : les trois blocs « Autres streams » dupliqués deviennent un seul (-77 lignes). Tri des ligues par niveau.
  - `js/main.js` + `index.html` : contrôle 3 états par ligue dans Favoris → Ligues (75 ligues, bouton ↺ par ligue, réinitialisation globale). Le pré-scraping de démarrage suit le classement.
  - **Couverture des sources** : listes `ESPN_LEAGUES` du client et du serveur fusionnées en une seule liste vérifiée (59 entrées, chaque endpoint testé HTTP 200) ; ajout de NWSL, Coupe du monde féminine, WNBA, Euroleague, WTA, Top 14, Premiership Rugby, F1, IndyCar, NASCAR, UFC, ATP, PGA côté client ; retrait des endpoints morts (`wwe/wwe`, `boxing/boxing`, `hockey/…professional-hockey-league` renvoient tous 400).
  - **WWE, AEW et boxe** : ESPN ne les expose pas du tout (absents de son répertoire de sports). Source trouvée : TheSportsDB `eventsday.php?d=…&s=Fighting` (CORS ouvert). `parseSportsDbEvents` (`js/scrapers.js`) + branchement client et serveur. Les faux événements WWE hebdomadaires ne sont plus fabriqués systématiquement : ils servent de repli quand la vraie source ne répond pas.
  - **PWHL** : aucune API publique trouvée (absente d'ESPN et de TheSportsDB) ; le scraper thepwhl.com existant reste la source.
  - **Alias** : les noms renvoyés par les API (« PGA TOUR », « Gallagher Prem », « Women's National Basketball Association », « IndyCar Series »…) tombaient dans « Autres ». Alias et noms d'affichage ajoutés des deux côtés.
  - **ESPN 403** : `scripts/scrape_schedule.mjs` n'obtenait plus rien (l'User-Agent par défaut de Node et les UA imitant un navigateur sont rejetés). User-Agent de robot avec URL de contact → 0 puis 16 matchs ESPN récupérés.
  - Tests : `tests/unit_leagues.test.js` (8 groupes) ajouté à `npm test` — niveaux, choix utilisateur, absence de doublon, **parité des listes ESPN et des alias entre client et serveur**, noms d'API reconnus, `parseSportsDbEvents`.
  - Vérifié dans Chromium : bascule d'une ligue en « ignorée » persistée et appliquée, 75 lignes réglables, aucune erreur JS, aucun débordement horizontal à 390 / 768 / 1440 px.
- 2026-09-02 - Sportsurge : les pages de match répondaient 404 depuis GitHub Actions (0 lecteur Sportsurge dans `data/streams.json`) parce que `parseSportsurge` résolvait les liens relatifs (`watch-63082-…-8/`) contre la page de ligue `/watch-<sport>-streams/` au lieu de la balise `<base href="https://v2.sportsurge.net/">` du site. Corrigé (`js/scrapers.js`) + test dans `tests/unit_scrapers.test.js`. Attendu au prochain run horaire : ~15 lecteurs par match Sportsurge.
- 2026-09-02 - Options → Réseau & Proxys : bouton « ▶️ Lancer le calcul sur GitHub » (ouvre la page GitHub Actions du workflow `scrape_streams.yml`, où « Run workflow » relance le pré-calcul) et bouton « 🔄 Recharger les liens serveur » (relit `data/streams.json` sans attendre la tranche de 5 min, puis refusionne la grille). État du cache serveur enrichi (liens, sources OK). Fichiers : `index.html`, `js/multiview.js` (`openStreamsWorkflow`, `reloadPrefetchedStreams`, `renderProxyStatus`), `js/main.js` (`loadPrefetchedStreams(force)`), `js/config.js` (`REPO_URL`, `STREAMS_WORKFLOW_URL`). L'app étant statique, le déclenchement direct via l'API GitHub (jeton requis) n'a pas été retenu.
- 2026-09-02 - Exploration des sites sources (accès Web) et corrections de l'extraction des liens/vidéos.
  - Constats (depuis un serveur / GitHub Actions) : Footybite bloque ses pages `/game/` (Cloudflare 403, l'accueil passe) ; Sportsurge passe depuis les runners GitHub mais pas depuis tous les centres de données ; Streameast (tous miroirs) répond 429 « error code 1015 » aux serveurs ; MLBBite et VIPLeague ne mettent aucun lecteur dans le HTML (VIPLeague : script obfusqué `stream.bun.min.js` + blob chiffré ; MLBBite : tableau `.streams-table-new` jamais rempli côté serveur) ; OnHockey : l'accueil est un frameset, la grille est dans `schedule_table.php` (403 sans Referer) ; Buffstreams et OnHockey pointent vers le lecteur `embedsports.me/<sport>/<slug>-stream-N` (exige un referrer et refuse l'attribut `sandbox`) ; Methstreams expose `allStreams` (embedindia.st, streame.center).
  - `js/scrapers.js` : `unwrapOnHockeyPlayer` (np_stream400.php / np_youtube.php → lecteur direct), heure OnHockey (GMT → Est), `isJunkStreamHost` + nettoyage (réseaux sociaux, clones partenaires « Watch on … », lecteurs `/?stream_id=` conservés), iframes nommées par leur hôte, MLBBite statut live/terminé + heure relative (« 15 minutes from now »), liens Streameast marqués `topLevel`.
  - `js/config.js` : OnHockey → `homepageHasMatches:false`, page `schedule_table.php`.
  - `js/ui.js` `renderFluxItem` : les liens `topLevel` s'ouvrent dans un nouvel onglet (↗).
  - `scripts/scrape_streams.mjs` : en-tête Referer par site, statistiques de requêtes (`fetch`), hôtes des lecteurs (`playerHosts`), pages de match OK/KO par source, `scrapeError` et `topLevel` conservés dans `data/streams.json`, un seul lien de repli « Page du match » par site.
  - `tests/unit_scrapers.test.js` (jsdom, sans réseau) ajouté à `npm test`.
  - Workflow `scrape_streams.yml` déclenché manuellement sur main (premier run OK : 113 matchs).
  - Restant : Footybite/Streameast/VIPLeague/MLBBite n'exposent pas de lecteur exploitable depuis un serveur ; leurs liens restent des pages à ouvrir dans un onglet. Sportsurge dépend de l'IP du runner.
- 2026-09-02 - Refonte de la recherche de liens (plus aucun flux trouvé depuis fin août : proxys CORS morts ou payants, domaines saisis/migrés).
  - `js/fetcher.js` (nouveau, pur, testé) : liste des transports (direct + proxys + proxy perso/clés API), détection des pages d'erreur renvoyées en HTTP 200, santé/ordre des proxys.
  - `js/utils.js` `fetchPage` réécrit : cache 45 s + dédoublonnage des requêtes, essai direct puis proxys en « hedging » (3 s), tous les transports essayés, proxys défaillants relégués 3 min.
  - `js/config.js` : domaines vérifiés (Sportsurge → v2.sportsurge.net, Streameast → v2.gostreameast.is, VIPLeague → vipleague.vg/live-now-streaming), miroirs (`SOURCE_MIRRORS`, surchargeables par `MIRRORS` dans domains.json), `applySourceUrl`, sous-pages par sport (`pages`), `sportOfLeague`, `getSourcePages`. Sources mortes supprimées (NFLBite doublon, Totalsportek saisi, Streamonsport devenu un blog).
  - `js/scrapers.js` : `extractStreamLinks(html, m)` extrait de `scrapeMatchFlux` (réutilisable côté serveur) ; parseurs Footybite, Sportsurge, Buffstreams, Methstreams, VIPLeague réécrits pour les formats 2026 (pages par ligue) ; nettoyage des faux liens ; `scrapeMatchFlux(m, force, deep)` lit aussi les pages du match sur les autres sources (`altUrls`).
  - `scripts/scrape_streams.mjs` + `.github/workflows/scrape_streams.yml` : pré-calcul horaire des liens côté serveur (réutilise les parseurs du client via jsdom) → `data/streams.json`, chargé par `loadPrefetchedStreams` (`js/main.js`) sans proxy. `js/package.json` (`type: module`) pour permettre l'import Node.
  - Options : section « Réseau & Proxys » (proxy personnalisé, clés API cors.sh / corsproxy.io, état des proxys et du cache serveur).
  - Tests : `tests/unit_fetcher.test.js` (21 cas) ajouté à `npm test` ; domaines des tests Playwright mis à jour.
  - Restant : Streameast (mirroirs 429/Cloudflare) ne fournit que la liste ; OnHockey inaccessible en direct (défi Cloudflare) mais passe via proxy ; l'affichage Firefox n'a pas pu être testé ici (téléchargement de Firefox bloqué).
- 2026-08-25 - Audit complet des domaines de streaming (mises à jour de `domains.json`, `js/config.js` et tests pour Sportsurge, Streameast, VIPLeague). Amélioration du filtrage des URLs de navigation dans `isMatchOrLeaguePage` (`js/scrapers.js`) et de la résolution d'iframe dans `resolveStreamUrl`. Ajout d'une superposition d'aide au Multiview dans `js/multiview.js` en cas de blocage d'intégration par le navigateur (Firefox / X-Frame-Options).
- 2026-08-24 - Updated `LEAGUE_ALIASES` in `js/db.js` and `scripts/scrape_schedule.mjs` to map full ESPN league names (e.g. English Premier League, Spanish LaLiga, Italian Serie A, German Cup, Portuguese Primeira Liga, MLB) to standard league keys recognized by `DEFAULT_LEAGUES` and `OTHER_LEAGUES`. Regenerated `data/schedule.json` to fix guide categorizations.
- 2026-06-08 - Audit complet du calendrier des ligues et mise à jour des domaines de streaming. Restauration des endpoints fonctionnels pour Streameast (naturallyyou.fit), OnHockey (onhockey.tv) et StreamOnSport (streamonsport.pro) dans domains.json, js/config.js et tests. Ajout du support de mise en forme et icônes pour AEW et MotoGP dans js/db.js.
- Fix Footybite stream extraction fallback in `scrapeMatchFlux` to parse DOM structure when Next.js payload is missing.
- Note for future scrapers: Added explicit site-specific fallback pattern inside `scrapeMatchFlux` to allow bypassing generic class-based extraction if Cloudflare blocks Next.js payloads.
- Update `scrape_schedule.yml` Github Action to trigger on `push` to `main`, ensuring `schedule.json` updates automatically and first load is fast.
- 2026-06-07 - Implemented advanced Next.js JSON payload parsing in `js/scrapers.js` for Footybite, Streameast, and MLBite to correctly extract match schedules from their new React-based architecture.
- 2026-06-07 - Enhanced `scrapeMatchFlux` to parse embedded stream links and iframes directly from script tags and Next.js payloads on individual match pages, bypassing Cloudflare 403 blocks on direct DOM loads.


* **2026-06-07**: Correction du bogue critique provoquant des erreurs 404 (GitHub Pages) dans le Multiview. Import et utilisation globale de `resolveUrl` au sein de `resolveStreamUrl` dans `js/utils.js` pour imposer des URLs de flux absolues strictes (`https://`).

- 2026-06-05 - Fixed scraper extraction issues by updating hardcoded base URLs for Footybite (`home.footybite.vc`), Totalsportek (`totalsportekz.com`), Buffstreams (`app.buffstreams.is/indexcracked29`), and VIPLeague (`www.vipleague.ws`).
- 2026-06-05 - Implemented dynamic domain resolution in `js/config.js` and `js/main.js` which fetches a remote `domains.json` file from the repository at startup to seamlessly update domains without requiring full app updates.
- 2026-06-03 - Fixed CI playwright extraction test failing due to Sportsurge/Methstreams scraping proxies returning wrapped HTML in `<pre>` instead of standard DOM output. Also handled `520` Cloudflare status codes. Updated base URLs for Buffstreams and VIPLeague to resolve endpoint fetch errors.
- 2026-06-03 - Updated GitHub Action workflows (`android-build.yml` and `tests.yml`) to use Node.js version 22 instead of 20, fixing the `@capacitor/cli` compatibility failure during the CI process.
- 2026-06-03 - Fixed tests.yml failure when downloading browsers or testing by updating Playwright GitHub Action to not be deprecated and fixed capacitor webDir setting.

# Worklog

## En cours

## Fait
- Update `scrape_schedule.yml` Github Action to change schedule to 09:00 UTC (4 AM EST).
- Fix Footybite stream extraction fallback in `scrapeMatchFlux` to parse DOM structure when Next.js payload is missing.
- Note for future scrapers: Added explicit site-specific fallback pattern inside `scrapeMatchFlux` to allow bypassing generic class-based extraction if Cloudflare blocks Next.js payloads.
- Update `scrape_schedule.yml` Github Action to trigger on `push` to `main`, ensuring `schedule.json` updates automatically and first load is fast.
- 2026-05-28 - Ajout d'attributs `aria-label` aux boutons avec des icônes uniquement dans `index.html`, `js/ui.js`, et `js/multiview.js` pour améliorer l'accessibilité.
- Optimizing array iterations (forEach, map, filter, find) to traditional for loops to improve performance and reduce resource usage.
- 2026-05-18 - Fix robust URL parsing using getDomain with a custom fallback and a new resolveUrl helper in js/config.js to resolve scrapers missing protocol bugs without error. Replaced naive new URL try/catch blocks in js/scrapers.js.
- 2026-05-16 - Déplacer bouton de mise à jour de la liste des streams et mettre en haut de la liste des streams, sans rond autour, juste l'icone, à côté, ajouter une icone de multiview qui ajoute un stream random du match (sauf si 4k stream dispo, mettre 4k stream). Aussi, 4k streams et Buffstream Flux toujours favoris en haut.
- 2026-05-16 - Fixed stream cache persistence issue where cached streamLinks were discarded upon page refresh. Updated `getApiFirstMatches` in `js/api.js` to eagerly inject saved `stream_cache` into newly fetched API matches before saving them into the local calendar cache.
- 2024-05-18 - Mettre "Manches" au lieu de "Temps" pour le baseball dans la section des scores.
- 2026-05-16 - Fixed URL matching/parsing in `js/scrapers.js` where `new URL` failed silently for base URLs lacking a protocol, preventing relative streams from resolving properly.
- 2026-05-16 - Enhanced `getDomain` in `js/config.js` with regex fallback to gracefully parse naked domains if `new URL` throws an exception, avoiding full URL returns when protocols are missing.
- 2024-05-16 - Correction du bug undefined BP/BC et mise à jour des statistiques par défaut (statsToCompare) dans js/ui.js pour correspondre aux Team Stats affichées par ESPN pour chaque sport (Soccer, Hockey, MLB, etc.), regroupées dans la section Voir les statistiques de la saison.

## Fait
- Fix Footybite stream extraction fallback in `scrapeMatchFlux` to parse DOM structure when Next.js payload is missing.
- Note for future scrapers: Added explicit site-specific fallback pattern inside `scrapeMatchFlux` to allow bypassing generic class-based extraction if Cloudflare blocks Next.js payloads.
- Update `scrape_schedule.yml` Github Action to trigger on `push` to `main`, ensuring `schedule.json` updates automatically and first load is fast.
- 2024-05-18 - Mettre "Manches" au lieu de "Temps" pour le baseball dans la section des scores.
- 2024-05-15 - Fixed premature stream scraping blocks by introducing a `hasEnoughStreams` check in `fetchSubPages` (background scraper) and `openMod` (foreground modal). This ensures matches that were checked too early and had 0 streams are eligible for re-scraping later, solving the missing streams issue.
- 2026-05-15 - Ajout de logs de debug détaillés (diagnosticScrape), persistance des streams manuels (via saveStreamCache), fonction globale copyToClipboard et affichage des logs multilignes dans le Multiview.
- 2024-05-15 - Fixed URL matching/parsing in `js/scrapers.js` for `MLBITE_URL` trailing slash replacement bug by checking `endsWith('/')` rather than blindly using string `.replace(/\/$/, '')`.
- 2024-05-15 - Refined match modal for game streaming in `js/ui.js` by hiding legacy header/footer, injecting a new close button into `wrapperHtml`, and fixing the async stream render target in `js/scrapers.js` to update the new split layout instead of overwriting the entire modal.
- 2024-05-15 - Redesigned stream modal layout (desktop and mobile) to use a 2-column view with stickied game info on the left (reusing live cards design) and stream links on the right.
- 2026-05-15 - Ajout de l'outil de diagnostic et extraction manuelle de flux dans la fenêtre de match. Refactorisation de `isMatchPair` vers `debugMatchPair` pour exposer la raison de l'échec de l'association.
- 2026-05-14 - Fixed URL matching/parsing in `js/scrapers.js` for `MLBITE_URL` trailing slash replacement bug by checking `endsWith('/')` rather than blindly using string `.replace(/\/$/, '')`.
- 2026-05-14 - Refined match modal for game streaming in `js/ui.js` by hiding legacy header/footer, injecting a new close button into `wrapperHtml`, and fixing the async stream render target in `js/scrapers.js` to update the new split layout instead of overwriting the entire modal.
- 2026-05-14 - Redesigned stream modal layout (desktop and mobile) to use a 2-column view with stickied game info on the left (reusing live cards design) and stream links on the right.
- Added auto-refresh mechanism for match streams when their status transitions to "live", bypassing stream caches and resetting the `streamsLoaded` state flag.
- Ajouter lien vers match sur ESPN dans modal des streams.
- À droite, mettre carte du match avec le 45 dégradé et logo.
- Ou mettre une photo ou un texte de preview du match si ESPN ou autre peut en fournir?

## Fait
- Fix Footybite stream extraction fallback in `scrapeMatchFlux` to parse DOM structure when Next.js payload is missing.
- Note for future scrapers: Added explicit site-specific fallback pattern inside `scrapeMatchFlux` to allow bypassing generic class-based extraction if Cloudflare blocks Next.js payloads.
- Update `scrape_schedule.yml` Github Action to trigger on `push` to `main`, ensuring `schedule.json` updates automatically and first load is fast.
- 2024-05-18 - Mettre "Manches" au lieu de "Temps" pour le baseball dans la section des scores.
- 2024-05-15 - Fixed "Autres Flux" display toggling in grid views and separated it into a dedicated collapsible section at the bottom of the "En direct" tab.
- 2024-05-15 - Removed arbitrary stream bounds limiting in `scrapeMatchFlux` and updated stream quantity threshold to 1000 so that every game will have every stream displayed.
- Added parsing logic in `js/api.js` for ESPN article text, photo, and web link.
- Modernized the `openMod` layout in `js/ui.js` to feature a stylish 45-degree gradient VS matchup card.
- Implemented logic in `fetchAndRenderModalStats` to overlay the ESPN article photo and text on the matchup card when available.
- Appended a new `Lire sur ESPN` button underneath the poster card.
- 2024-05-15 - Flattened stream link layout in js/ui.js to a single line and removed "Recommandé/Alternatif" text to simplify UI.
- Increase stream limits in scrapeMatchFlux (js/scrapers.js) to display all streams for sources with large volumes.
- 2024-05-13 - Update Footybite URL to army.footybite.to in js/config.js and js/ui.js.
- 2024-05-13 - Fixed missing leagues in the EPG guide by moving DEFAULT_LEAGUES to db.js and handling case-insensitive logic in UI display sorting.
- Refonte visuelle de la carte de match dans `js/ui.js` (mode Scoreboard).
- Simplification du bouton Multivision dans la liste de flux en icône carrée simple.
- Correction du scroll mobile pour `#modal-left-col` (retrait de la couleur de fond fixe et du sticky) dans `styles.css`.
- 2024-05-18 - Implemented frontend UI for the Scraper Investigator inside the options modal, integrating interactive DOM selection to dynamically build custom scraper rules bypassing X-Frame-Options.
